# Handoff State

_Last updated: 2026-05-12. Phase 11 #55 (`qb_w2_summary`) AND Phase 12 #59 (attachments) BOTH CLOSED. Tool count 99 → 103. 742 → 798 tests green. **Phase 11 is now ENTIRELY closed.** Carried forward: Phase 18 #84/#85/#86 (connection robustness, closing date, MCP prompts), Phase 13–17 coverage gaps (custom fields, banking primitives, sales orders, sales tax, time tracking, inventory, statement charges). The previously-deferred Phase 11 #54 live SCF verification AND Phase 18 #82 live host_query verification both still need an operator pass against the real `VR Tax & Consulting Inc..qbw` file with the QBXML logger (#83) enabled — neither blocks new feature work._

## Last Session Summary

- **Phase 11 #55 shipped — `qb_w2_summary` (W-2 prep via PayrollSummaryReportQueryRq).** Full end-to-end NEW wire surface (the operator's two-task batch needed both #55 and #59, neither rideable on existing wire plumbing):
  - **Builder** ([src/qbxml/builder.ts](src/qbxml/builder.ts)): new `buildPayrollSummaryReportRequest` — conservative schema-order subset (`PayrollSummaryReportType` → `ReportPeriod` → `SummarizeColumnsBy` → `ReportEntityFilter`). NO `ReportBasis` child — payroll reports are inherently cash. Defensive `expect(xml).not.toContain("<ReportBasis>")` pin in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) catches a future copy-paste regression from `buildReportRequest`.
  - **Manager** ([src/session/manager.ts](src/session/manager.ts)): new `runPayrollSummaryReport(params)` method — wraps the builder, sends, extracts via existing `extractReportData` path (which routes live ReportData row trees through `adaptLiveReportRet`).
  - **Sim handler** ([src/session/simulation-store.ts](src/session/simulation-store.ts)): new `handlePayrollSummaryReportQuery` dispatched BEFORE the `endsWith("QueryRq")` catch-all. Walks Employee store, picks the year from `ReportPeriod.FromReportDate` / `ToReportDate` (or most-recent seeded year if neither bound supplied), looks up each employee's `PayrollYTDByYear[year]` block, masks SSN to last 4 (`XXX-XX-1234`), surfaces optional state fields (TX has no state income tax → state boxes absent). Returns `EmployeeWagesTaxesRet` rows + grand `Totals`. Cross-year `ReportPeriod` rejects with 3120. Unsupported `PayrollSummaryReportType` (anything other than `EmployeeWagesTaxesAdjustments`) rejects with 3120.
  - **Sim seed** (in `seedData()`): three Employee records (Alice/IL, Bob/CO, Carla/TX) with sim-only `PayrollYTDByYear` extension covering 2024 + 2025. Withholdings computed against typical employer rates (FICA SS 6.2%, FICA Med 1.45%, federal ~10-15%) so figures look plausible. Carla (TX) carries `StateAbbreviation: "TX"` but NO `StateWages` / `StateIncomeTaxWithheld` — exercises the optional-state-fields branch.
  - **Parser** ([src/qbxml/parser.ts](src/qbxml/parser.ts)): added `EmployeeWagesTaxesRet` to arrayElements (single hit must surface as array for the tool layer's row walk).
  - **Status codes** ([src/util/qb-status-codes.ts](src/util/qb-status-codes.ts)): new synthetic **9003** ("edition does not support QB Payroll" — Pro builds without Plus) + **9004** ("payroll subscription required or not active" — empty-result path).
  - **Tool** ([src/tools/reports.ts](src/tools/reports.ts)): `qb_w2_summary({taxYear?, employeeFullName?, employeeListId?})`. Two-stage gating: (a) PRE-FLIGHT edition probe via `session.getHostInfo()` — Pro rejects with 9003 BEFORE wire round trip; (b) POST-WIRE empty-result rejects with 9004 (descriptive `statusMessage` distinguishes "no employees matched" from "subscription off"). Maps `EmployeeWagesTaxesRet` rows onto W-2 box numbers: `box1_wagesTipsOtherComp` / `box2_federalIncomeTaxWithheld` / `box3_socialSecurityWages` / `box4_socialSecurityTaxWithheld` / `box5_medicareWages` / `box6_medicareTaxWithheld` / `box16_stateWages` / `box17_stateIncomeTax`. Default `taxYear = current year - 1`. Wire-side period is always Jan 1 → Dec 31 of the resolved year. **Boxes 7-15 / 18-20 (SS tips, allocated tips, dep care, codes A-W, box 14 other, local taxes) are NOT surfaced** — those require per-payroll-item box mapping the SDK doesn't expose; documented loudly on tool surface + DECISIONS.md.
  - **25 new tests** in [tests/w2-summary.test.ts](tests/w2-summary.test.ts) + 1 schema-order pin: builder emit-order + no-ReportBasis + ListID-precedence; sim handler 3-employee + TX no-state + EntityFilter scope + 2025 year picker + grand totals + statusCode 1 empty-scope + cross-year 3120 + unsupported type 3120; tool default + override + scope by name/listId + 9004 + 9003 (via patched `getHostInfo`) + PremierAccountant passes + error wrapper humanReadable.

- **Phase 12 #59 shipped — attachments (`qb_attachment_add` / `_list` / `_delete`).** Full new wire surface for the Attachable family:
  - **New tool file** ([src/tools/attachments.ts](src/tools/attachments.ts)): three tools + new `registerAttachmentTools` + wired in [src/index.ts](src/index.ts).
  - **Sim handler** ([src/session/simulation-store.ts](src/session/simulation-store.ts)): new `handleAttachableAdd` dispatched BEFORE the `endsWith("AddRq")` catch-all. Validates `FileReference.FullPath` exists on disk via `fs.statSync` (sync — matches `processRequest`'s sync contract); validates `ObjectRef.{TxnID|ListID}` target exists by walking every non-Attachable store; derives `FileName` / `FileSize` / `FileExtension` from disk + reverse-looks-up `ObjectType` / `FullName` from the target entity; defaults `AttachmentType: "Normal"`; normalizes `ShowAsImage` from string/bool. Returns `AttachableRet` wrapped under `data: { AttachableRet: ... }` so `extractResponseData` + `flattenEntityArray` work uniformly.
  - **`ObjectFilter`** branch added to the GENERIC `handleQuery` (entity-type-agnostic — scopes by `ObjectRef.{TxnID|ListID}` on the entity). Only Attachable carries an ObjectRef in this server's first cut; other entities are unaffected (filter is silently skipped for entities without ObjectRef).
  - **Delete** rides the existing generic `handleListDel` path (Attachable is a list entity, not a transaction — uses `ListDelRq` with `ListDelType="Attachable"`). No new delete handler needed.
  - **Parser** ([src/qbxml/parser.ts](src/qbxml/parser.ts)): added `AttachableRet` to arrayElements.
  - **Tool layer enforces:** (a) absolute-path required (relative paths reject with 3120 upfront — a relative path resolved against QB Desktop's CWD is rarely intended); (b) mutual-exclusivity on `txnId` / `listId` for add (both supplied or neither rejects with 3120); (c) at-most-one filter on list (`txnId` / `targetListId` / `attachableListId`).
  - **Read-only gate** composes through `addEntity` / `deleteEntity`'s `assertWritable` (9001). **Idempotency** via `addEntityIdempotent` on add (9002 on key conflict).
  - **No new manager methods** — `addEntity` / `queryEntity` / `deleteEntity` cover all three tool surfaces.
  - **31 new tests** in [tests/attachments.test.ts](tests/attachments.test.ts) across 6 layers: (1) sim handleAttachableAdd — Invoice + Customer + Bill happy paths, file-not-found 500, missing FileReference 3120, missing ObjectRef 3120, ObjectRef without TxnID/ListID 3120, unknown target 500; (2) ObjectFilter — by TxnID, by ListID, empty result, unfiltered; (3) sim delete — happy + 500; (4) qb_attachment_add — txnId + listId happy paths, mutual-exclusivity (both/neither), relative-path rejection, idempotency replay + 9002 conflict, read-only gate 9001, file-not-found surfacing with humanReadable; (5) qb_attachment_list — all-unfiltered, txnId scope, targetListId scope, attachableListId fetch, multi-filter rejection 3120; (6) qb_attachment_delete — happy, unknown ID 500, read-only gate 9001. Tests use `mkdtempSync` + `writeFileSync` for stub files; cleanup in `afterEach`.

- **Docs updated**: [src/index.ts](src/index.ts) instructions block extended with `qb_w2_summary` + `qb_attachment_*` category lines; idempotency-keyed-tools list now includes `qb_attachment_add`. [README.md](README.md) tool count `99 total` → `103 total`, architecture diagram (`99 tools` → `103 tools`), `qb_w2_summary` row added to Reports & Queries, new "Attachments" section with three rows. Two new entries at the top of [DECISIONS.md](DECISIONS.md) (#55 + #59 design rationale).

## Verify Before Continuing

Re-run if the tree has been touched (skip if next session starts within hours):

- [ ] `npm run build` → exit 0.
- [ ] `npm test` → `Test Files 35 passed | Tests 798 passed`.
- [ ] `"" | & node dist/index.js` → exit 0, `Mode: simulation` printed; banner does NOT print "QBXML debug log: enabled" when `QB_DEBUG_QBXML` is unset.
- [ ] **(Windows + QB) FIRST live exercise of `qb_w2_summary` against `VR Tax & Consulting Inc..qbw`.** Highest priority of the new live verifications because the wire shape of `EmployeeWagesTaxesRet` is best-guess from SDK reference docs, not pinned against real QB. Set `QB_DEBUG_QBXML=1` in the Claude Desktop config's `env` block, call `qb_w2_summary({taxYear: 2024})` (the operator's most recent completed tax year before today's 2026-05-12). Read the per-day log file. Capture the actual `EmployeeWagesTaxesRet` shape — if QB emits per-employee data via a row tree under `ReportData` (like P&L / BS / SCF do), the sim's flat shape won't match and `extractReportData` will return `{}`. The fix path: add a payroll-specific branch to `adaptLiveReportRet` in [src/qbxml/parser.ts](src/qbxml/parser.ts) similar to the SCF route added with #54. The schema-order pin in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) only catches request-side bugs.
- [ ] **(Windows + QB) FIRST live exercise of `qb_attachment_add` against `VR Tax & Consulting Inc..qbw`.** Lower priority than W-2 because the operator's edition probably supports Attached Documents (Premier / Enterprise / Accountant generally do), but still worth verifying. Pick any small test PDF (e.g. a scanned receipt) and a known-existing transaction (e.g. a recent Bill or Check), call `qb_attachment_add({txnId, filePath, note: "live smoke test"})`. Verify the response's `AttachableRet.ObjectRef.ObjectType` matches the txn type. Then `qb_attachment_list({txnId})` to confirm the attachment surfaces. Then `qb_attachment_delete({attachableListId})` to clean up. If any of these fails with a QB-side error referencing "Attached Documents not enabled", surface that in the next handoff — it would gate every operator's use of the tool until they enable the feature in QB Desktop's Preferences → Integrated Applications → Attached Documents Service.
- [ ] **(Windows + QB) Carried — Phase 18 #82 live host_query verification (deferred from prior session).** Set `QB_DEBUG_QBXML=1`, call `qb_host_query({})`. Capture the actual `ProductName` / `MajorVersion` / `SupportedQBXMLVersionList` / `QBFileMode` QB returns. Compare against the seeded sim shape (`Premier Accountant Edition 2024` / `34` / 19-entry list / `SingleUser`). If the live response surfaces fields beyond the seven seeded (e.g. `Beta`, `IsHostedOnline`, edition-flag booleans), extend the `HostInfo` type in [src/session/manager.ts](src/session/manager.ts) + the `normalizeHostInfo` helper to surface them. Verify the derived `edition` matches expectations.
- [ ] **(Windows + QB) Carried — Phase 11 #54 live SCF verification (also deferred).** Call `qb_statement_of_cash_flows({ fromDate: "2024-01-01", toDate: "2024-12-31", basis: "Accrual" })`; if `Sections` come back empty despite the underlying transactions, compare the logged `TextRow` / `SubtotalRow` / `TotalRow` labels against `CASH_FLOWS_SECTION_NAMES` + close-label patterns in [src/qbxml/parser.ts](src/qbxml/parser.ts) `adaptLiveReportRet`.

## Next Task

Operator's pick. Phase 11 is now FULLY CLOSED with #55. Phase 12 has only #59 in scope today and that's also closed; future Phase 12 work would be net-new feature additions. Carried forward:

- **Phase 18 — 3 robustness items remaining.**
  - **#84 connection robustness** — auto-reconnect on transient `QBXMLRP2` failures, graceful `EndSession` / `CloseConnection` on `SIGTERM` / `SIGINT`. Partial — the SIGTERM/SIGINT half landed during the live-mode work in [src/index.ts](src/index.ts) `gracefulShutdown`. Auto-reconnect on transient errors is the remaining work. Likely uses an exponential backoff (250ms → 500ms → 1s → fail) capped at 3 retries on the QB-server "not connected" status code (`0x80040408`).
  - **#85 closing date / year-end lock** — `qb_closing_date_set({ closingDate, password })` and `qb_closing_date_get`. SDK exposure for SETTING (vs reading) the closing date is limited by edition + version; verify SDK surface before scoping. May need to fall back to documenting "set in QB Desktop" with a clear error path.
  - **#86 MCP prompts** — workflow-bundle prompts surfaced via the MCP `prompts` API alongside `tools`. Bridges to the operator's existing `credit-card-qb-batch` / `trial-balance-workup` / `cc-statement-validator` skill workflows. Composes naturally with the new infrastructure (could prompt-bundle the W-2 prep flow now that #55 exists).
- **#67 `qb_session_status`** — health check / probe tool from Phase 14. Composes naturally with #82 (Host) — surface `mode`, `companyFile`, `qbxmlVersion`, `connected`, `readOnly`, plus the cached `HostInfo` (avoiding the round trip if cached). Small follow-on if the operator wants a single "tell me everything about the current session" tool.
- **Phase 13–17** — broader coverage gaps. None block the closed items but each represents an entire workflow surface the MCP can't touch today:
  - Phase 13 (data model): customer email/phone in invoice list (#60), custom fields / DataExt (#61), sub-customer/job hierarchy (#62), memo full-text search (#63).
  - Phase 14 (safety/DX): dry-run mode (#64), better error surfaces (#65), audit log (#66 — Enterprise-only, gates on `host.isEnterprise`), session status (#67).
  - Phase 15 (practice-specific): trial balance export (#68), tax line mapping (#69), engagement profitability (#70), client packet (#71).
  - Phase 16 (architecture): generic `qb_transaction_list` (#72), streaming responses (#73), MCP-side caching (#74).
  - Phase 17 (coverage): banking primitives (#75 — would unblock a real bank-rec workflow), sales orders (#76), sales tax (#77), time tracking (#78 — unblocks #70), vehicle mileage (#79), inventory adjustments (#80), statement charges (#81).

## Context Notes

- **#55 `qb_w2_summary` is the FIRST tool to use synthetic statusCode 9003 + 9004.** The HANDOFF previously reserved 9003+ as "future synthetic codes"; this session burned 9003 (edition unsupported) and 9004 (subscription required). Both have humanReadable entries in [src/util/qb-status-codes.ts](src/util/qb-status-codes.ts). Future synthetic codes start at 9005.
- **#55 `PayrollYTDByYear` is sim-only** — does NOT exist on real `EmployeeRet`. Anyone reading Employee records via `qb_employee_list` will see the field in sim and not in live. Documented as a sim seed extension in DECISIONS.md; not a contract that survives the live switch.
- **#59 `ObjectFilter` is the new generic filter on `handleQuery`** — entity-type-agnostic but currently only meaningful for Attachable. Any future entity type that adds an `ObjectRef` field gets the filter for free.
- **#55 + #59 both ship sim-side complete + live verified-by-construction.** Same pattern as #54 (SCF), #82 (Host), #83 (logger). The first live exercise will reveal whether the sim's response shape matches QB's actual wire emit; if not, the fix is a row-tree adapter (P&L / BS / SCF route in `adaptLiveReportRet`) for #55, and a metadata-field extension to `handleAttachableAdd` for #59. The schema-order pins in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) only protect REQUEST-side bugs.
- **#59 file path validation lives in TWO places by design:** sim handler validates via `fs.statSync` (mirrors live QB's "file not found" 500 error so observers see the same failure mode in both modes), AND the tool layer rejects relative paths upfront (clearer error than letting `fs.statSync` resolve to whatever's in the QB process's CWD). Documented in DECISIONS.md.
- **#59 file BYTES are NEVER transferred through the SDK.** QBXMLRP2 takes a path string; QB Desktop reads the file from disk during `ProcessRequest`. The operator must keep the source file alive until the wire call completes (real QB then has its own copy). Sim doesn't copy anything — sim is for testing tool wiring, not for testing QB's file storage.
- **Carried gotchas (unchanged from prior handoffs):**
  - **Three lists must stay in sync** for transaction types: `buildDeleteRequest` in [src/qbxml/builder.ts](src/qbxml/builder.ts), inline `isTransaction` in `manager.deleteEntity` ([src/session/manager.ts](src/session/manager.ts)), and `isTransactionType` in [src/session/simulation-store.ts](src/session/simulation-store.ts). Canonical 16-type set: Invoice, Bill, Estimate, SalesReceipt, CreditMemo, PurchaseOrder, JournalEntry, Deposit, Transfer, Check, BillPaymentCheck, BillPaymentCreditCard, ReceivePayment, SalesOrder, CreditCardCharge, CreditCardCredit.
  - **AR-side `Customer.Balance` discount math is correct; AP-side is NOT.** `applyTxnApplications` / `reverseReceivePaymentApplication` (AR) move `Customer.Balance` by `appliedSum + discountSum`. The mirror functions `applyBillPayment` (AP) still move `Vendor.Balance` by `appliedSum` only. Future `qb_bill_write_off` will need the parallel fix.
  - Dispatch-order in `processRequest`: non-entity-typed `*QueryRq` / `*ModRq` branches (`PayrollSummaryReportQueryRq` (NEW with #55), `CustomDetailReportQueryRq`, `GeneralDetailReportQueryRq`, `TransactionQueryRq`, `ClearedStatusModRq`) MUST precede the `endsWith("QueryRq")` / `endsWith("ModRq")` catch-alls. Same rule for `AttachableAddRq` (NEW with #59) — must precede the `endsWith("AddRq")` catch-all.
  - Synthetic statusCode sentinels: `9001` = read-only gate; `9002` = idempotency-key conflict; **9003 = edition unsupported (NEW); 9004 = payroll subscription required (NEW)**; 9005+ reserved. All mapped in [src/util/qb-status-codes.ts](src/util/qb-status-codes.ts).
  - Iterator wire names diverge: `iterator`/`iteratorID` on request, `iteratorRemainingCount`/`iteratorID` on response.
  - fast-xml-parser does NOT decode numeric character entities — use `decodeXmlEntities` in [parser.ts](src/qbxml/parser.ts).
  - Live verification requires `C:\nvm4w\nodejs\node.exe` v20.20.2 (system PATH v22 breaks winax).
  - QBXMLRP2 cannot OPEN a `.qbw`, only attach to one QB Desktop has already loaded.
  - BillPayment* total is on `TotalAmount`, not `Amount` — coalesce `TotalAmount ?? Amount` for BillPaymentCheck / BillPaymentCreditCard; Check / CreditCard* / Transfer use `Amount` directly.
  - Bill ItemLineAdd in test fixtures should pass explicit `Amount` (or use Rate) — the sim's `convertLineAddToRet` computes `Amount = Quantity * Rate` and Bill ItemLineAdd's `Cost` field isn't picked up there.
