# QuickBooks Desktop MCP — Fix List

Comprehensive list of fixes required to make the server work flawlessly for personal use. Ordered by recommended execution sequence (simulation correctness first so later changes can be tested in dev, live COM last because it requires a Windows + QB Desktop box to verify).

---

## Phase 1 — Simulation correctness (fix dev environment first)

So that everything below can actually be verified without a Windows box.

- [x] **15.** Add `EntityFilter`, `TxnDateRangeFilter`, `PaidStatus`, `RefNumber`, `ModifiedDateRangeFilter` handling to `simulation-store.handleQuery` — currently silently ignored, so transaction list queries return ALL records regardless of filter.
- [x] **17.** Convert input `*LineAdd` arrays to `*LineRet` arrays in simulation responses (with `TxnLineID`, computed `Amount = Quantity * Rate`) so downstream tools see proper line breakdown.
- [x] **16.** Compute `Subtotal`, `SalesTaxTotal`, `BalanceRemaining`, `AppliedAmount`, `IsPaid` in `simulation-store.handleAdd` for invoices/bills/estimates so simulated responses match real QB shape.
- [x] **18.** Update customer/vendor balances in simulation store when invoices/bills/payments are added/applied/deleted, so AR/AP aging reports reflect activity in dev.
- [x] **22.** Multi-store Item simulation: split the single `Item` store into `ItemService` / `ItemInventory` / `ItemNonInventory` / `ItemOtherCharge` / `ItemGroup` stores keyed by `ItemType`, return correct `*Ret` element name (`ItemServiceRet` etc.) so parser's `arrayElements` set actually matches.

## Phase 2 — Item request types (touches schema everywhere)

- [x] **2.** Fix Item request types: replace generic `ItemQueryRq` / `AddRq` / `ModRq` with per-type variants (`ItemServiceQueryRq`, `ItemInventoryQueryRq`, `ItemNonInventoryQueryRq`, `ItemOtherChargeQueryRq`, `ItemGroupQueryRq`) in [tools/items.ts](src/tools/items.ts) and route through manager based on `itemType` arg.
- [x] **3.** Fix Item delete to use correct `ListDelType` subtype (`ItemService`, `ItemInventory`, etc.) — currently sends `'Item'` which QB rejects.

## Phase 3 — Transaction completeness (the workflows that matter)

- [x] **4.** Add `ExpenseLineAdd` and `ItemLineAdd` support to `qb_bill_create` — accept `lines: [{accountName, amount, memo}]` and `lines: [{itemName, quantity, cost}]`, so bills actually post to GL accounts.
- [x] **5.** Add `AppliedToTxnAdd` support to `qb_payment_receive` — accept `appliedTo: [{txnId, amount, discountAmount?}]` so payments actually close out invoices and reduce customer balances.
- [x] **7.** Implement `qb_bill_update` tool (`BillModRq`) — header fields plus `ExpenseLineMod` / `ItemLineMod` support, register in [index.ts](src/index.ts).
- [x] **6.** Add `InvoiceLineMod` support to `qb_invoice_update` — accept `lines` arg with optional `txnLineID` (existing line) or `'-1'` (new line) and build `InvoiceLineMod` blocks.
- [x] **8.** Add `qb_payment_apply` tool — apply an existing unapplied `ReceivePayment` to specific invoices via `ReceivePaymentMod` + `AppliedToTxnMod`.
- [x] **9.** Add `qb_bill_pay` tool — record `BillPaymentCheck` or `BillPaymentCreditCard` against existing bills (currently no way to mark a bill as paid).

## Phase 4 — Missing tools / coverage gaps

- [x] **10.** Add `qb_account_delete` / `qb_account_make_inactive` — currently only list/add/update; needed to deactivate unused accounts (QB usually disallows hard delete if transactions exist, so `make_inactive` is the correct primary).
- [x] **11.** Add `qb_employee_delete` / `make_inactive` (currently only list/add/update).
- [x] **12.** Add missing transaction tools: ~~`qb_sales_receipt_*`~~ (done 2026-04-26), ~~`qb_credit_memo_*`~~ (done 2026-04-26), ~~`qb_purchase_order_*`~~ (done 2026-04-26), ~~`qb_journal_entry_*`~~ (done 2026-04-26).
- [x] **13.** Add `qb_estimate_update`, `qb_estimate_delete`, `qb_estimate_convert_to_invoice` tools — currently only list/create.
- [x] **30.** Add `Class`, `Terms`, `PaymentMethod`, `SalesRep`, `CustomerType`, `VendorType` list tools — needed because invoice/bill creation references these by `FullName` but there's no way to list/discover them.

## Phase 5 — Reporting (currently mostly fake)

- [x] **14.** Implement real `CompanyQueryRq` in `qb_company_info` — return company name, legal name, address, fiscal year start, tax form, etc., not just session state.
- [x] **19.** Implement `asOfDate` filtering in `qb_ar_aging` and `qb_ap_aging` — currently the param is decorative; should filter open transactions by date and bucket by 0-30 / 31-60 / 61-90 / 90+ days.
- [x] **20.** Add proper P&L and Balance Sheet report tools (`GeneralSummaryReportQueryRq` with `ReportType=ProfitAndLossStandard` / `BalanceSheetStandard`) — current "reports" are just account-balance rollups.
- [x] **21.** Add date-range support to `qb_balance_summary`, and group by `AccountType` in canonical QB order (Bank, AccountsReceivable, OtherCurrentAsset, ... Equity, Income, COGS, Expense).

## Phase 6 — Plumbing, validation, ergonomics

- [x] **23.** Fix `QB_SIMULATION` env semantics in [session/manager.ts:51-53](src/session/manager.ts#L51-L53) — currently `QB_SIMULATION=false` on Windows still simulates unless `QB_LIVE=1` is also set; either honor `QB_SIMULATION=false` alone or document the actual rule and align README.
- [x] **24.** Remove dead code: `parseQBXMLResponse` import in [session/manager.ts:27](src/session/manager.ts#L27), `buildSingleRequest` export in [qbxml/builder.ts:66](src/qbxml/builder.ts#L66), `QBXMLRequestBody` import in [qbxml/builder.ts:9](src/qbxml/builder.ts#L9), and the useless ternary `isTransaction ? id : id` at [simulation-store.ts:214](src/session/simulation-store.ts#L214).
- [x] **25.** Wrap `session.queryEntity` / `addEntity` / `modifyEntity` / `deleteEntity` calls in tool handlers with try/catch — translate `QBXMLResponseError` into structured tool error responses (`isError: true` with `statusCode` + `statusMessage`) instead of letting them propagate as raw exceptions.
- [x] **26.** Add status code mapping table for common QB errors (3120 missing field, 3170 modify failed, 3260 insufficient permission, 500 not found, etc.) so tool errors are user-readable.
- [x] **27.** Add `IteratorID` / `IteratorRemainingCount` support to large queries (Customer, Invoice, Bill, Item) — real QB caps at ~500 rows and returns an iterator handle for pagination.
- [x] **28.** Validate `AccountType` enum in `qb_account_add` against QB's allowed values (`Bank`, `AccountsReceivable`, `OtherCurrentAsset`, `FixedAsset`, `OtherAsset`, `AccountsPayable`, `CreditCard`, `OtherCurrentLiability`, `LongTermLiability`, `Equity`, `Income`, `CostOfGoodsSold`, `Expense`, `OtherIncome`, `OtherExpense`, `NonPosting`).
- [x] **29.** Add minimal input validation: email format, phone, postal code, ISO date strings (`YYYY-MM-DD`) on date fields — currently any string passes through to QB which then rejects with cryptic errors.

## Phase 7 — Live mode (last, requires Windows + QB Desktop to verify)

- [x] **1.** Implement live `QBXMLRP2` COM connection in [session/manager.ts](src/session/manager.ts) (add `winax` / `node-activex` dep, replace throws in `openSession` / `sendRequest` with real `OpenConnection2` / `BeginSession` / `ProcessRequest` / `EndSession` / `CloseConnection` calls, wire `parseQBXMLResponse` for live responses). _(Closed 2026-05-09. winax 3.4.2 prebuilt for Node 20.20.2 extracted via `npm install`. `node scripts\verify-live-connection.mjs` returned full company data for `VR Tax & Consulting Inc.` (EIN 84-2215991, Form 1120S). Live MCP transport verified end-to-end via [scripts/exercise-mcp-live.mjs](scripts/exercise-mcp-live.mjs): 10/10 read-only tools (qb_company_info, qb_account_list, qb_customer_list, qb_vendor_list, qb_item_list, qb_invoice_list, qb_bill_list, qb_employee_list, qb_class_list, qb_terms_list) returned structured live data after a schema-ordering fix — see notes in HANDOFF.md.)_
- [x] **34.** Add `qb_company_open` tool — accept `companyFile` arg (absolute or UNC path), close any active session (`EndSession` + `CloseConnection`), update `QBSessionManager.config.companyFile`, open a new session against the new file. In simulation mode it just swaps the path string and resets `SimulationStore` to fresh seed (so the LLM observes a "different company" with its own books). Required so the operator can move between client `.qbw` files in one chat session without restarting the server. Behavior must be observationally identical between live and simulation per NF1. Register in [src/index.ts](src/index.ts), add to `instructions` block, document in README session-tools table. _(Closed 2026-05-09. Added `QBSessionManager.switchCompanyFile(path)` + `getCompanyFile()` in [src/session/manager.ts](src/session/manager.ts); tool registered alongside qb_company_info in [src/tools/reports.ts](src/tools/reports.ts). Sim-mode response carries `simulationStoreReset: true` so the LLM observes the discontinuity. Sim-fidelity tradeoff documented in [DECISIONS.md](DECISIONS.md#2026-05-09--company-switching-reseeds-the-simulation-store).)_
- [x] **35.** Add `qb_company_list` tool — read a configured root folder (new env `QB_COMPANY_ROOT`, default to the directory of `QB_COMPANY_FILE`), enumerate `*.qbw` files, return `[{ companyFile, displayName, sizeBytes, modifiedAt }]`. Pure filesystem operation; works identically in live and simulation. Lets the LLM discover the operator's client list by name without the operator typing UNC paths. Register in [src/index.ts](src/index.ts), add to `instructions` block, document in README. _(Closed 2026-05-09. Resolves search root in priority order: `root` arg → `$QB_COMPANY_ROOT` → `dirname($QB_COMPANY_FILE)`. Returns sorted-by-modifiedAt-desc results so the most-recent client surfaces first. `.env.example` documents the new var.)_
- [x] **36.** Add a fresh `tests/company-switching.test.ts` (Vitest) covering: (a) `qb_company_open` swaps file path and seeds a fresh store, (b) data created against company A is not visible after switching to company B, (c) switching back to A returns A's seed (NOT the prior session's mutations — confirms reset is intentional and documented), (d) `qb_company_list` returns expected entries from a temp dir of fixture `.qbw` files. Document the "switching resets sim state" decision in [DECISIONS.md](DECISIONS.md) — it's a deliberate sim-fidelity tradeoff (real QB persists; sim doesn't). _(Closed 2026-05-09. 11 new tests in [tests/company-switching.test.ts](tests/company-switching.test.ts) covering all four scenarios + schema-level empty-string rejection, env-var fallback, root-arg override, missing-root error, missing-directory error, empty-directory case, and end-to-end list→open round-trip. Total suite now 189 passing across 6 files.)_

## Phase 8 — Project hygiene

- [x] **31.** Add `tests/` directory with Vitest: round-trip tests for builder→parser, simulation-store CRUD per entity, filter handling, and tool integration tests through the MCP server transport. _(Closed 2026-04-27. 5 test files in `tests/` cover 178 assertions: `iterator.test.ts` (27), `input-validation.test.ts` (44), `error-shape.test.ts` (47) port the matching `.mjs` harnesses; new `qbxml-roundtrip.test.ts` (40) and `simulation-store.test.ts` (20) cover the builder→parser + CRUD/filter gaps. Run via `npm test`. `verify-item23-env-matrix.mjs` kept as standalone script per [DECISIONS.md](DECISIONS.md#2026-04-27-vitest-for-the-test-harness-keep-env-matrix-as-a-standalone-mjs-script). The 5 `.mjs` harnesses are kept and complementary — they verify the built `dist/`, while Vitest verifies `src/`.)_
- [x] **32.** Add `.gitignore` (`node_modules/`, `dist/`, `*.log`, `.env`), `.env.example` documenting all `QB_*` vars, and run `npm run build` to verify `dist/` produces working node entry. _(Closed 2026-04-27. `.gitignore` already covered required entries plus `.env.local` + `.DS_Store`; `.env.example` shipped under Item 23. Smoke test: `node dist/index.js </dev/null` prints 5-line startup banner with `Mode: simulation` and exits 0.)_
- [x] **33.** Update README tool count and tables once new tools (~~bill_update~~, ~~payment_apply~~, ~~bill_pay~~, ~~account_delete~~, ~~sales_receipt~~/~~credit_memo~~/~~PO~~/~~JE~~, ~~estimate_update/delete/convert~~, ~~supporting list tools~~) are added. _(Closed 2026-04-26 when JE README work landed.)_

---

## Phase 9 — Critical bug fixes (operator-blocking, P0)

Reported by the operator on 2026-05-09 during hands-on Claude Desktop testing against the live `VR Tax & Consulting Inc..qbw` file. These block core CPA workflows.

- [x] **37.** Fix `qb_pnl_report` — currently returns `statusCode -1` "QuickBooks found an error when parsing the provided XML text stream" regardless of params (with/without dates, with/without basis). Single most-used report in any tax + bookkeeping practice; until this works the tool can't answer "what did we make last month" without walking invoices + sales-receipts + credit-memos + JEs (and even then misses JE-only postings). Diagnose: enable QBXMLRP2 request logging, capture the failing payload, compare child order against QB's own `<xs:sequence>` for `GeneralSummaryReportQueryRq` (the 2026-05-09 schema-order class of bug). Pin the fix in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) once root cause is known. _(Closed 2026-05-09. Two halves: (a) schema-order — `buildReportRequest` emitted `ReportBasis` at child position 3, QB requires position 15 after `SummarizeColumnsBy`/`IncludeSubcolumns`; pinned in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts). (b) row-tree adapter — even after the parse error cleared, live returned `<ReportData>` rows instead of `{Sections, Totals}` so payloads were empty; new `adaptLiveReportRet` in [src/qbxml/parser.ts](src/qbxml/parser.ts) translates. Verified live against `VR Tax & Consulting Inc..qbw` FY2024: TotalIncome 516216.51, TotalExpenses 501880.01, GrossProfit 507216.51, NetIncome 14336.5; BS reconciles 341828.33 = 197863.26 + 143965.07. `qb_balance_sheet_report` got the adapter for free. 199/199 tests pass; [scripts/exercise-mcp-live.mjs](scripts/exercise-mcp-live.mjs) extended with 5 report probes; design rationale in [DECISIONS.md](DECISIONS.md#2026-05-09--live-report-shape-adapted-at-the-parser-layer-not-at-each-tool).)_
- [x] **38.** Fix `qb_balance_summary` `asOfDate` filter — currently silently ignored. Returns current snapshot even when called with `asOfDate` and `simulationMode: false`, so the operator gets wrong data without warning. The `asOfNote` field admits this in plain English ("Balance reflects current snapshot, not the requested date range. Historical reconstruction requires walking transactions per account, pending Phase 5 P&L work."). Either implement historical reconstruction or remove the date params and the misleading note. Recommended: implement, since balance-as-of is also table-stakes. _(Closed 2026-05-09. Tool params replaced: `fromDate`/`toDate` → `asOfDate` + `basis`. AS/LI/EQ figures sourced from `runReport("BalanceSheetStandard", { toDate: asOfDate, basis })`; INC/EXP figures from `runReport("ProfitAndLossStandard", { toDate: asOfDate, basis })` (lifetime through asOfDate). Per-account totals bucketed back into the 16-way canonical AccountType order via name→type lookup from a single AccountQuery. NonPosting accounts fall back to `Account.Balance` (not in either report — they don't post to GL). Bucket logic extracted as exported `buildBalanceSummary` for direct unit testing. 15 new tests in [tests/balance-summary.test.ts](tests/balance-summary.test.ts) (8 unit on the helper, 7 end-to-end via session.runReport). Misleading `asOfNote`/`asOfDateRange` dropped. Live exerciser extended with 3 new probes (28/28). Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-09--qb_balance_summary-sources-asliep-from-balancesheetstandard-and-incexp-from-profitandlossstandard). Tradeoff: sim's `BalanceSheetStandard` reads `Account.Balance` for AS/LI/EQ (snapshot — `asOfDate` advisory there); P&L walk IS date-bounded in both modes. Build clean, 214/214 tests green, tool count unchanged at 74.)_
- [x] **39.** Pagination DX — calling any list tool with `paginate: true` and no `maxReturned` returns "There is a missing element: MaxReturned." The tool already knows it needs `MaxReturned` the moment `paginate` flips true. Default `maxReturned` to 500 (QB's effective per-batch cap) when `paginate` is enabled, document the cap in tool descriptions, surface the iterator semantics in error messages. _(Closed 2026-05-09. Coalesce added at the tool handler in all four tools that expose `paginate` — `qb_customer_list`, `qb_invoice_list`, `qb_bill_list`, `qb_item_list`. `iteratorID` alone (mid-pagination Continue) also triggers the default. Explicit `maxReturned` still wins. Tool descriptions updated on all four. 14 new tests in [tests/iterator.test.ts](tests/iterator.test.ts) Layer 8 pin the contract: spy on `session.queryEntityPaginated` to assert `filters.MaxReturned === 500` for the default path, `=== 50` for explicit override, untouched for the non-paginated `queryEntity` path. Error-message rewrite for callers who explicitly trigger "missing element: MaxReturned" deferred to Item 65 per HANDOFF scope. Build clean, 228/228 tests green (was 214 + 14 new). Tool count unchanged at 74.)_

## Phase 10 — High-priority features (operator's top-10 list)

Operator-prioritized list of "if I were maintainer and you were customer" — top features beyond the P0 bugs.

- [x] **40.** `qb_transaction_list_by_account` — any GL account, any date range, with running balance. Without this, cannot reconstruct income, audit a suspicious balance, or pull anything QB calls a "Transaction Report." Wire as `TransactionQueryRq` with `AccountFilter` + `TxnDateRangeFilter` + sort by date; compute running balance server-side from the response. _(Closed 2026-05-09. New tool registered in fresh [src/tools/transactions.ts](src/tools/transactions.ts), wired in [src/index.ts](src/index.ts) (tool 75). Manager exposes `queryTransactions(filters)` that wraps `TransactionQueryRq` via the generic `queryEntity` path — keeps the "tools never construct QBXML directly" rule intact. Sim adds `handleTransactionQuery` cross-type fanout in [src/session/simulation-store.ts](src/session/simulation-store.ts): walks Bill/Check expense+item lines, Invoice/SalesReceipt/CreditMemo income lines via `resolveLineAccount`, and JE debit/credit lines with account-natural-direction sign convention. Sim emits LINE-LEVEL postings only — implicit AR/AP/Bank counter-postings are documented as not surfaced (live QB returns the full posting tree). Running balance computed in the tool handler per HANDOFF math: `openingBalance = Account.Balance − Σ period postings`, then walks forward; exact when toDate ≥ now, approximate (overstated by post-period postings) for historical windows — documented limitation. `includeRunningBalance:false` opts out of the AccountQueryRq round trip. 16 new tests in [tests/transaction-list.test.ts](tests/transaction-list.test.ts) cover sim line-level fanout, JE sign convention (natural-debit Rent Expense vs natural-credit Sales Revenue), date filter, MaxReturned cap, ListID-form AccountFilter resolution, missing AccountFilter (3120 reject), tool error shape, and running-balance math against the seeded snapshot — Rent Expense closes back to its 24000 seed balance after the 3 period postings (2000+2100+500), Sales Revenue closes to 185000 after +700/-150. Plus the schema-order regression pin in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) for `TransactionQueryRq` (MaxReturned → TxnDateRangeFilter → AccountFilter). 244/244 tests green (was 228 + 16 new). Build clean.)_
- [x] **41.** Line-level detail in list responses — invoice / sales-receipt / bill / credit-memo / PO / estimate `*_list` responses currently return header totals only. Add `includeLineItems: boolean` arg (default `false` to preserve current shape). To answer "what services drove April revenue" you'd otherwise need 27 round trips per month. Wire via `IncludeLineItems` in the existing `*QueryRq`. _(Closed 2026-05-09. Scope expanded to seven list tools — added `qb_journal_entry_list` because its description already promised lines on every row and the strip-by-default change would have made it lie. Sim handleQuery now strips `*LineRet` / `*LinesRet` keys via `/Line(s?)Ret$/` regex by default; preserves them when IncludeLineItems is truthy (boolean `true` for in-process callers, string `"true"` for the wire form). Header-derived totals (Subtotal, AmountDue, BalanceRemaining, IsPaid, TotalDebit/Credit, AppliedAmount) survive the strip. AppliedToTxnRet (relationship array) survives — its key doesn't match the regex. `qb_estimate_convert_to_invoice` updated to internally pass `IncludeLineItems: true` so the convert flow doesn't lose its line set. Schema-order positions pinned in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) for InvoiceQueryRq + BillQueryRq + JournalEntryQueryRq + (looped) Estimate/SalesReceipt/CreditMemo/PurchaseOrder QueryRqs. 42 new tests in [tests/include-line-items.test.ts](tests/include-line-items.test.ts) + builder-emit-order.test.ts. 286/286 tests green, build clean, tool count unchanged at 75.)_
- [x] **42.** Read-only session flag — `qb_session_connect({ readOnly: true })` lets an agent run diagnostic work with zero mutation risk. QBXMLRP2 supports per-call enforcement; gate every `*_add` / `*_update` / `*_delete` / `*_apply` tool to throw a structured error when session is read-only. Big win for confidence in unattended automation. _(Closed 2026-05-10. Single chokepoint inside `QBSessionManager` ([src/session/manager.ts](src/session/manager.ts)): private `readOnly` flag + `setReadOnly` / `isReadOnly` accessors + private `assertWritable(operation)` helper called at the entry of `addEntity` / `modifyEntity` / `deleteEntity` / `executeBatchAdd`. Throws new `QBReadOnlyError` (synthetic statusCode 9001) BEFORE any envelope is built — surfaces through the existing tool-side error wrapper as `isError: true` with `humanReadable` via [src/util/qb-status-codes.ts](src/util/qb-status-codes.ts). Zero per-tool churn — all 47 mutation call sites across 14 tool files automatically inherit the gate. `qb_session_connect` ([src/tools/reports.ts](src/tools/reports.ts)) accepts `readOnly: z.boolean().optional()`; defaults to false on every call (fresh `qb_session_connect()` always re-enables writes). `qb_company_info` surfaces current `readOnly` state. Flag persists across `openSession`/`closeSession`. `switchCompanyFile` deliberately ungated. 25 new tests in [tests/read-only-session.test.ts](tests/read-only-session.test.ts) cover the manager state machine, gate firing on each of the 4 mutation methods, reads staying open, tool-surface propagation through `qb_session_connect` / `qb_company_info`, and structured-error surfacing through `qb_customer_add` / `_update` / `_delete` + `qb_journal_entry_batch_create`. Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-10--read-only-session-flag-gates-at-the-session-manager-not-per-tool). 327/327 tests green. Tool count unchanged at 76 (no new tool — flag-only change).)_
- [x] **43.** `qb_journal_entry_batch_create({ entries: [...] })` — atomic (all-or-nothing). Replaces the Excel intermediate the operator's `credit-card-qb-batch` skill currently produces. Wire as a single QBXML envelope with multiple `JournalEntryAddRq` blocks under `<QBXMLMsgsRq onError="stopOnError">`. Same pattern reused by item 58 (batch invoice/SR). _(Closed 2026-05-10. New tool registered in [src/tools/journal-entries.ts](src/tools/journal-entries.ts); 1–100 entries per call. Upfront balance validation rejects unbalanced batches before any wire I/O. Compensating delete in REVERSE post order on mid-batch failure — tracks `rolled-back` (delete succeeded) vs `orphaned` (delete itself failed; surfaces TxnID for manual cleanup). Generalized the plumbing for #58: [src/session/simulation-store.ts](src/session/simulation-store.ts) `processRequest` now walks array-valued request keys and honors `onError="stopOnError"`; [src/qbxml/parser.ts](src/qbxml/parser.ts) captures `@_requestID` per response; new `session.executeBatchAdd(entityType, entries)` in [src/session/manager.ts](src/session/manager.ts) returns positionally-aligned posted/failed/skipped slots. 16 new tests in [tests/journal-entry-batch.test.ts](tests/journal-entry-batch.test.ts) cover envelope plumbing, parser requestID capture, sim multi-request + stopOnError, full-success path, single-entry batch, upfront-validation rejection, floating-point tolerance, schema bounds (1–100), full rollback path, and orphaned path via mocked delete failure. New multi-request envelope structure pin in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts). Tool count 75 → 76. Design rationale logged in [DECISIONS.md](DECISIONS.md#2026-05-10--qb_journal_entry_batch_create-defaults-to-atomic-with-auto-rollback-compensating-delete). 302/302 tests green.)_
- [x] **44.** `qb_1099_summary` and `qb_1099_detail` — for a 2,000-client practice issuing 1099s every January, conspicuous absence. QBXML: `Form1099QueryRq` (1099-MISC + 1099-NEC). Returns vendor + threshold + tracked amounts. _(Closed 2026-05-10. Two tools registered in fresh [src/tools/form-1099.ts](src/tools/form-1099.ts), wired in [src/index.ts](src/index.ts) (tools 77 + 78). Implementation diverges from the literal `Form1099QueryRq` plan: aggregates server-side from existing typed `Bill` + `Check` queries via `session.queryEntity` rather than wiring a new wire request — keeps the "tools never construct QBXML directly" rule, identical sim/live behavior, no schema-order risk to verify on a Windows box. Vendor classification driven by `IsVendorEligibleFor1099` + `Vendor1099Type` ('NEC' default, 'MISC' opt-in). Threshold defaults to $600 (IRS general TY2024+). Defaults `taxYear` to last completed year. Card payments excluded per IRS rule. Limitation: doesn't honor QB Preferences' per-account 1099 box mapping — every payment to an eligible vendor counts toward the threshold (more permissive than QB's Form1099 wizard, documented in the tool description). Three new 1099-eligible vendors added to the seed (Joe Contractor / Sarah Designer LLC / ACME Property Mgmt) with TIN + address fields; existing vendors got an explicit `IsVendorEligibleFor1099: false`. 24 new tests in [tests/form-1099.test.ts](tests/form-1099.test.ts) cover pure helpers (date-window resolution, vendor classification, line totaling), the aggregation engine (Bill + Check sum, ineligible-vendor exclusion, out-of-window exclusion, empty result), the summary tool (sort order, threshold filter, includeBelowThreshold, formType filter, explicit fromDate/toDate override, address/taxId surfacing, default taxYear), and the detail tool (per-transaction breakdown, vendorListId scope, vendorFullName scope, no-match success, formType filter). Tests use `session.addEntity` to create Bill/Check fixtures rather than seeding them globally — the seed-only-vendors approach kept the existing 327 tests green. 351/351 tests green. Tool count 76 → 78. Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-10--qb_1099_summary--qb_1099_detail-aggregate-from-typed-bill--check-queries-not-form1099queryrq).)_
- [x] **45.** ~~Memorized / recurring transaction CRUD~~ — **CLOSED 2026-05-10 as SDK-blocked.** Verified against qbxmlops130/140 schemas (the qbwc/qbxml master mirrors of Intuit's official SDK XSDs): `MemorizedTransactionListQueryRq` / `MemorizedTxnListQueryRq` / Add / Mod / Del / Execute — **none exist in the QBXML SDK at any version.** The only mention of `memorizedTxn` in the entire 2.77 MB schema is as an enumeration value for `rowType` inside `ListDeletedQueryRq` (i.e. you can query deletion records of memorized templates, not the live list). Intuit's long-standing posture: memorized transactions are managed exclusively through QB Desktop's UI (Ctrl+M on an open transaction) and auto-executed by QB's internal scheduler; the SDK doesn't expose the recurring/memorized list to integrators. Workflow stand-in to address the operator's underlying pain (monthly retainer billing without QB's memorized system): build `qb_invoice_duplicate(sourceTxnId, overrides)` as a Phase 12 follow-up — reads a prior invoice's lines, submits an `InvoiceAddRq` with new TxnDate / RefNumber. Same outcome as right-click "Use" on a memorized template, no SDK gap. Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-10--memorized-transactions-not-exposed-by-qbxml-sdk-45-closed-sdk-blocked).
- [x] **46.** Bank reconciliation primitives — **CLOSED 2026-05-10. Scope narrowed after SDK research.** Original HANDOFF premise was wrong in both directions: `ReconcileQueryRq` / `ReconcileDetail` report type / `LastReconciledDate` field — none exist in the QBXML schema. What DOES exist (and the HANDOFF missed) is `ClearedStatusModRq` — the actual atomic write primitive bank reconciliation is built on (flips one txn or one split line between `Cleared` / `NotCleared` / `Pending`). Shipped `qb_cleared_status_update` ([src/tools/reconciliation.ts](src/tools/reconciliation.ts)) wrapping it. Targets the seven bank-affecting transaction types (Check, BillPaymentCheck, BillPaymentCreditCard, Deposit, Transfer, CreditCardCharge, CreditCardCredit); calls against Invoice/Bill/JE/etc. return statusCode 3120 distinguishing "wrong txn type" (sim probes other-txn-type stores) from 500 "unknown TxnID". Read-only gate composes (9001). Naturally idempotent — no idempotency key arg (would add no value; flipping Cleared on an already-Cleared txn is a server no-op). Read-side (which txns are uncleared) requires `CustomDetailReportQueryRq` with `IncludeColumn=ClearedStatus` — the only QBXML path that returns cleared-status — and pairs structurally with Phase 11 #56 (which has the same custom-report plumbing). Read-side carry: new item **#56a** below. Until that lands, the operator pairs QB Desktop's reconciliation UI (visual: which items need clearing) with `qb_cleared_status_update` bulk calls through an agent (replaces clicking every line by hand). 23 new tests in [tests/reconciliation.test.ts](tests/reconciliation.test.ts) cover the builder schema-order pin, sim handler (NotCleared default on bank-affecting add, header- and line-level flips on Check/Deposit/Transfer/BillPaymentCheck, 500 vs 3120 distinction, enum validation, missing TxnID, missing line ID), manager method (assertWritable gate fires, 9001 sentinel, idempotency cache untouched), and tool surface (happy paths, error surfacing with humanReadable, read-only gate compose). 395/395 tests green. Tool count 78 → 79. Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-10--bank-reconciliation-sdk-surface--clearedstatusmodrq-is-the-actual-primitive-46-closed-with-narrowed-scope).
- [x] **47.** Idempotency keys on creates — optional `idempotencyKey: string` arg on every `*_create` / `*_add` tool. Server stores recent `(key → created TxnID/ListID)` pairs in the simulation store and the live session manager (in-memory, scoped to current `companyFile`); retrying with the same key returns the original result instead of duplicating. Prevents duplicate invoices on agent retry / network glitch. _(Closed 2026-05-10. Single chokepoint inside `QBSessionManager` mirroring the #42 read-only gate template — new `addEntityIdempotent` / `executeBatchAddIdempotent` methods + `Map<key, {entityType, payloadFingerprint, result, createdAt}>` cache, FIFO-bounded at 1000 entries, cleared on `switchCompanyFile`. Fingerprint = SHA-256 of canonicalized JSON (recursive sorted-key normalization, array order preserved). New `QBIdempotencyKeyConflictError` (synthetic statusCode 9002) thrown when same key is reused with a different payload — surfaces through the existing tool error wrapper with humanReadable from [src/util/qb-status-codes.ts](src/util/qb-status-codes.ts). Tool surface: 16 `*_create` / `*_add` handlers across 14 files gain optional `idempotencyKey: z.string().min(1).optional()`; on replay the response carries `idempotentReplay: true`. Failed creates do NOT poison the cache — next retry can fix the underlying problem. Batch tool ([src/tools/journal-entries.ts](src/tools/journal-entries.ts) `qb_journal_entry_batch_create`) only caches full-success outcomes (partial failures would replay through the rollback path and re-attempt deletes of already-removed TxnIDs — documented loudly). `qb_estimate_convert_to_invoice` skips the IsAccepted flip on replay (the original call already ran it; re-running with stale editSequence would fail). Server `instructions` block in [src/index.ts](src/index.ts) extended with an idempotency section listing every keyed tool. 21 new tests in [tests/idempotency-keys.test.ts](tests/idempotency-keys.test.ts) cover the manager state machine (miss / replay / conflict / cross-entityType / different-keys / array-order sensitivity), cache lifecycle (failure-non-poisoning, cross-openSession persistence, switchCompanyFile clears, FIFO eviction at 1000), batch idempotency (full-success replay, partial-failure non-caching, reorder conflict), and tool surface (qb_customer_add propagation + idempotentReplay flag, conflict surfacing with statusCode 9002, no-key fall-through to existing behavior, qb_journal_entry_batch_create end-to-end + upfront-validation non-caching). 372/372 tests green (was 351 + 21). Tool count unchanged at 78 (no new tools — flag-only change). Design rationale logged in [DECISIONS.md](DECISIONS.md#2026-05-10--idempotency-cache-lives-at-qbsessionmanager-fingerprint-matches-on-replay-only-caches-successful-creates).)_

## Phase 11 — Remaining missing reports

CPA-weekly reports beyond Phase 10 priorities.

- [x] **48.** `qb_customer_balance_detail` — every open + closed transaction per customer, in date order with running balance. Aging summary tells you who owes, not why. QBXML: `CustomerBalanceDetailReportQueryRq`. _(Closed 2026-05-11 with #51 as a paired-pair. Shipped as a COMPOSITE over typed entity queries (`InvoiceQueryRq` + `ReceivePaymentQueryRq` + `CreditMemoQueryRq`) rather than as a new wire request — three round trips regardless of customer count, in-process grouping by `CustomerRef.FullName`, running-balance math via new `buildEntityBalanceSection` helper (mirror of `buildGeneralLedgerSection`). The HANDOFF's "TransactionQueryRq per customer/vendor + running-balance walk client-side" path was rejected because the sim's `handleTransactionQuery` requires `AccountFilter` and emits LINE-LEVEL postings only (implicit AR counter-postings not materialized) — same limitation that prevented `qb_transaction_list_by_account` from surfacing AR activity. The composite over typed entity queries reaches the AR posting directly via `Invoice.Subtotal+SalesTaxTotal` / `ReceivePayment.TotalAmount` / `CreditMemo.TotalAmount`. New `extractOriginalTxnAmount(txn, txnType)` helper in [src/tools/reports.ts](src/tools/reports.ts) handles per-txn-type amount resolution. `customerListId` → `customerFullName` canonicalization before passing to `EntityFilter` (sim's match-by-ListID path doesn't hit stored CustomerRefs added via `addEntity` with only FullName). JournalEntry postings to AR are NOT walked (JE lines don't carry customer ref reliably in sim — visible via `qb_transaction_list_by_account` on the AR account directly). 27 new tests in [tests/customer-balance-detail.test.ts](tests/customer-balance-detail.test.ts) across 4 layers (helper math, `extractOriginalTxnAmount` per txn type, single-customer scope + scope errors, multi-customer aggregation + pruning + caps). Tool count 89 → 90. See DECISIONS.md 2026-05-11.)_
- [x] **49.** `qb_sales_by_customer_summary` and `qb_sales_by_customer_detail` — pricing analysis, capacity planning, lost-client identification. QBXML: `GeneralSummaryReportQueryRq` / `GeneralDetailReportQueryRq` with `ReportType=SalesByCustomerSummary` / `Detail`. _(Closed 2026-05-10. Paired delivery — the summary half rides the existing `GeneralSummaryReportQueryRq` infrastructure (sim whitelist + new `ReportEntityFilter` arg on `buildReportRequest` + new "flat-summary" fork in `adaptLiveReportRet` for the no-PnL/no-BS-labels live shape); the detail half introduces the new `GeneralDetailReportQueryRq` wire request end-to-end (new `buildGeneralDetailReportRequest`, new `extractGeneralDetailReportData` that reuses `adaptLiveCustomDetailReportRet` for the row-tree adapter, new `handleGeneralDetailReportQuery` sim handler, new `processRequest` dispatch branch before the `endsWith("QueryRq")` catch-all, new `runGeneralDetailReport` manager method). Sim aggregation: walks Invoice + SalesReceipt + CreditMemo line stores by `CustomerRef.FullName`, sums `line.Amount` (matches QB's actual report — sales tax doesn't inflate), CreditMemo lines subtract. Summary returns `customers: [{customerName, total}]` sorted desc + `totalSales`; detail returns per-line rows with TxnType / Date / Num / Name / Memo / Item / Quantity / Rate / Account / Amount / TxnID sorted by Customer → Date → TxnID, CreditMemo rows emit with negative Amount. Customer scope via `customerName` / `customerListId` (server-side `ReportEntityFilter`). Live adapter for the detail report reuses the CustomDetailReport row-tree translator — verified-by-construction structurally but not yet live-validated against a real QB Desktop (caveat documented in the tool description). 29 new tests in [tests/sales-by-customer.test.ts](tests/sales-by-customer.test.ts) across 5 layers (sim summary handler, sim detail handler, live flat-summary adapter, tool surface summary, tool surface detail) + 2 schema-order pins in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) (`GeneralSummaryReportQueryRq` with `ReportEntityFilter`, full `GeneralDetailReportQueryRq` emit order). Tool count 83 → 85. 504/504 tests green. Design rationale in [DECISIONS.md](DECISIONS.md#2026-05-10--phase-11-49-qb_sales_by_customer_summary--qb_sales_by_customer_detail-ships-the-generaldetailreportqueryrq-wire-infrastructure). The new `GeneralDetailReportQueryRq` infrastructure is now load-bearing for the planned #50/#52/#54 detail variants — each future tool plugs in as ~50 LOC of sim-handler branch + tool wrapper.)_
- [x] **50.** `qb_sales_by_item_summary` and `qb_sales_by_item_detail` — same as above, by item. _(Closed 2026-05-11. Paired delivery mirroring #49 — summary half wraps `GeneralSummaryReportQueryRq` with `ReportType=SalesByItemSummary` and new `ReportItemFilter` plumbing on `buildReportRequest` (slotted at position 4, between `ReportEntityFilter` and `SummarizeColumnsBy` — pinned in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts)); detail half rides the existing `GeneralDetailReportQueryRq` infrastructure with the new sim handler branch in `handleGeneralDetailReportQuery`. New private builders in [src/session/simulation-store.ts](src/session/simulation-store.ts): `buildSalesByItemSummary` (walks Invoice + SalesReceipt + CreditMemo `*LineRet`, groups by `line.ItemRef.FullName`, lines without ItemRef drop, sort desc) and `buildSalesByItemDetail` (same walk, sort by Item → TxnDate → TxnID, CreditMemo Amount sign-flipped). Live adapter reused from #49's flat-summary fork (for summary) and `adaptLiveCustomDetailReportRet` row-tree translator (for detail). 17 new tests in [tests/sales-by-item.test.ts](tests/sales-by-item.test.ts) across 4 layers: (1) sim summary handler — per-item rollup, sort desc, CreditMemo subtraction, no-item line drop, date filter, itemFilter scope, empty window; (2) sim detail handler — per-line emit, sort by Item → Date → TxnID, scope, columns metadata, unsupported reportType rejection; (3) tool surface summary — flat items list, scope by itemName, schema rejection; (4) tool surface detail — per-line rows, scope, empty result, CreditMemo negative-Amount. Tool count 85 → 87. See DECISIONS.md 2026-05-11.)_
- [x] **51.** `qb_vendor_balance_detail` — analog to customer balance detail. QBXML: `VendorBalanceDetailReportQueryRq`. _(Closed 2026-05-11 with #48 as a paired-pair. AP mirror of #48 — walks `Bill` + `BillPaymentCheck` + `BillPaymentCreditCard` per vendor with the same `buildEntityBalanceSection` running-balance math. **Key vendor-specific behavior:** `Bill.AmountDue` is decremented on every payment, so `extractOriginalTxnAmount` walks the line set (`ExpenseLineRet + ItemLineRet`) for the original face value — a fully-paid bill still surfaces its original +$X impact on AP (pinned by the "Bill paid in full still surfaces its original face value" test). `BillPaymentCheck` / `BillPaymentCreditCard` post `TotalAmount ?? Amount` (HANDOFF gotcha). `VendorCredit` is NOT walked (no VendorCredit tool exists in this server's first cut — credits applied through bill payments still surface via `BillPaymentCheck.DiscountAmount` on the underlying bill). `BillQueryRq` passes `IncludeLineItems: true` because Phase 10 #41 strips lines by default; pinned by the existing emit-order test. 15 new tests in [tests/vendor-balance-detail.test.ts](tests/vendor-balance-detail.test.ts) across 3 layers (Bill original-amount extraction, single-vendor scope + scope errors, multi-vendor aggregation + pruning + caps). Tool count 90 → 91. See DECISIONS.md 2026-05-11.)_
- [x] **52.** `qb_expense_by_vendor_summary` and `qb_expense_by_vendor_detail` — `GeneralSummaryReportQueryRq` / `Detail` with `ReportType=ExpensesByVendorSummary` / `Detail`. _(Closed 2026-05-11. Expense-side mirror of #50 — same paired delivery shape. Summary half walks Bill + Check + CreditCardCharge `ExpenseLineRet` + `ItemLineRet`, groups by vendor name (Bill: `VendorRef.FullName`; Check / CCCharge: `PayeeEntityRef.FullName` via `resolveExpensePayeeName`), sums `line.Amount`, sort desc. Detail half emits per-line rows sorted by Vendor → TxnDate → TxnID with Account resolved via `resolveLineAccount(direction="expense")` (fallback 'Uncategorized Expense'). Adjacent fix forced by this work: added `CreditCardCharge` + `CreditCardCredit` to `isTransactionType` in [src/session/simulation-store.ts](src/session/simulation-store.ts:3699) and to the parallel `isTransaction` array in [src/qbxml/builder.ts](src/qbxml/builder.ts) — both were missing despite being in `BANK_AFFECTING_TXN_TYPES`, which silently disabled `convertLinesAddToRet` for CC txns. CLAUDE.md's transaction-entities list updated to match. Strict improvement — also fixes `qb_pnl_report` and `qb_balance_summary` CC walks in sim (previously silently empty for sim-seeded CC charges). 16 new tests in [tests/expense-by-vendor.test.ts](tests/expense-by-vendor.test.ts) across 4 layers: sim summary handler + detail handler + tool summary + tool detail. Caveat documented loudly: sim doesn't filter by underlying account's AccountType (matches #49's simplification — Check posting to a Fixed Asset counts as "expense to vendor"). Tool count 87 → 89. See DECISIONS.md 2026-05-11.)_
- [x] **53.** `qb_general_ledger` — period-bounded, account-filterable. Foundation report. _(Closed 2026-05-10. Shipped as a COMPOSITE over the existing `TransactionQueryRq` + `AccountQueryRq` primitives — NOT as the originally-scoped `GeneralDetailReportQueryRq` wire request. Rationale: `GeneralLedger` is not a valid `CustomDetailReportType` enum value per the QBXML SDK (the HANDOFF's "compose on top of CustomDetailReportQueryRq" suggestion would have failed live with statusCode 3120), and rolling a brand-new `GeneralDetailReportQueryRq` builder would introduce schema-order risk for marginal gain on a personal-tool surface. The composite reuses verified primitives that already run cleanly in live mode (the same plumbing `qb_transaction_list_by_account` rides on). Tradeoff: N round trips for N matching accounts in live mode (~100s for a 200-account chart at typical QBXMLRP2 latency) — mitigated by `accountType` / `accountName` scoping. Tool: [src/tools/reports.ts](src/tools/reports.ts). Args: `fromDate?`, `toDate?`, `accountName?`, `accountListId?`, `accountType?`, `basis?`, `maxAccounts?` (default 200), `maxRowsPerAccount?` (default 500), `includeEmpty?` (default false — empty sections pruned). Returns `{ fromDate, toDate, basis, accountCount, totalRowCount, sections: [{ accountName, accountListId?, accountType, openingBalance, closingBalance, periodChange, count, transactions: [...rows with RunningBalance], truncated?, error? }], warnings? }`. NonPosting accounts are always excluded from chart-wide fanout (warning surfaced when any are dropped); explicitly-named NonPosting accounts go through but return empty. Section-level error isolation — a single account's query failure surfaces as `section.error` without poisoning the rest. RunningBalance math matches `qb_transaction_list_by_account` exactly: `openingBalance = Account.Balance − Σ period postings`, then walks forward; exact when `toDate ≥ now`. Pure `buildGeneralLedgerSection(account, rows)` helper exported for unit testing without an MCP transport. 21 new tests in [tests/general-ledger.test.ts](tests/general-ledger.test.ts) across 5 layers (helper math, single-account scope, accountType + multi-account fanout, date filter + truncation, defensive section-level error isolation). Tool count 82 → 83. 475/475 tests green. Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-10--qb_general_ledger-shipped-as-composite-over-transactionqueryrq--accountqueryrq-not-as-a-new-wire-type).)_
- [x] **54.** `qb_statement_of_cash_flows` — `GeneralSummaryReportQueryRq` `ReportType=StatementOfCashFlows`. _(Closed 2026-05-12. Indirect-method SCF wrapping `GeneralSummaryReportQueryRq` with `ReportType=StatementOfCashFlows` — request-side rides the same `buildReportRequest` path as P&L / BS / SalesByCustomerSummary (no schema-order risk; pinned in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts)). Returns canonical 3-section shape (Operating Activities / Investing Activities / Financing Activities) plus totals `NetCashIncrease` / `CashAtBeginningOfPeriod` / `CashAtEndOfPeriod`. **Sim implementation** ([src/session/simulation-store.ts](src/session/simulation-store.ts) `buildStatementOfCashFlows`): Operating section walks `NetIncome` (from `buildPnLReport` over the same window) + ΔAR (= Invoice line totals + SalesTaxTotal − ReceivePayment.TotalAmount − CreditMemo line totals; row total = `−ΔAR`, because AR increase = USE of cash) + ΔAP (= Bill line totals − BillPayment*.TotalAmount; row total = `+ΔAP`, because AP increase = SOURCE of cash). Investing section walks period postings to FixedAsset + OtherAsset accounts via new `walkNonPnlAccountActivity` helper (Bill/Check/CreditCardCharge `ExpenseLine` + `ItemLine` → debit; JE `JournalDebitLine` / `JournalCreditLine` → debit/credit); per-account row total = `(credit − debit)`. Financing section walks LongTermLiability + Equity on the same convention (liability/equity increase = SOURCE of cash → positive CF). Zero-net accounts pruned. Cash totals: `CashAtEndOfPeriod` = Σ(Bank.Balance) snapshot from Account store; `CashAtBeginningOfPeriod` = `CashAtEnd − NetCashIncrease` (derived — sim has no historical Account.Balance series; documented loudly on the tool surface and in DECISIONS.md). Sim's narrower indirect-method model documented as a limitation: no inventory / prepaid / accrued / depreciation add-back (real QB does these). **Live adapter** ([src/qbxml/parser.ts](src/qbxml/parser.ts) `adaptLiveReportRet`): new `CASH_FLOWS_SECTION_NAMES` map covers section-open TextRow label variants (`OPERATING/INVESTING/FINANCING Activities`, plus `Cash from/provided by operating activities` etc. for older / locale variants); new `isScf` detection branch (skips P&L + BS routes); per-section close-label list covers `Total <activity>` / `Net cash provided by <activity>` / `Net cash used in <activity>` / `Net cash from <activity>` / `Cash provided by <activity>` — pinned in tests. SCF totals branch reads `Net cash increase for period` (and `decrease` / `increase in cash` / `change in cash` variants) for `NetCashIncrease`, `Cash at beginning of period` for `CashAtBeginningOfPeriod`, `Cash at end of period` for `CashAtEndOfPeriod`; falls back to summed section subtotals when grand-total rows aren't emitted. **23 new tests** — 22 in [tests/statement-of-cash-flows.test.ts](tests/statement-of-cash-flows.test.ts) covering sim handler (canonical shape, Operating ΔAR happy path + payment offset, Operating ΔAP, Investing JE debit to FixedAsset, Financing JE credit to LongTermLiability, Financing equity contribution + distribution net, date filter exclusion, cash-totals reconcile by construction, CashAtEnd = Σ(Bank.Balance), section subtotals sum to NetCashIncrease, zero-net account pruning) + tool surface (canonical sections, malformed-date rejection, error-path statusCode + humanReadable) + live adapter (SCF row tree routing, section accumulation + close on Net-cash-provided-by, totals extraction, "Net cash used in" close-label variant, "Cash from operating activities" section-open variant, fallback to summed subtotals when grand-total row missing) + 1 in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) (StatementOfCashFlows emit-order pin). Tool count 97 → 98. 660 → 683 tests green. Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-12--qb_statement_of_cash_flows-ships-sim-side-only-live-adapter-verified-by-construction). **Live verification deferred** — adapter labels (OPERATING / INVESTING / FINANCING Activities + Net cash provided by/used in close patterns) are best-guess from QBXML SDK reference docs and not yet validated against a real QB Desktop SCF dump. First live run against `VR Tax & Consulting Inc..qbw` should capture the actual TextRow / SubtotalRow / TotalRow labels and, if they diverge, extend `CASH_FLOWS_SECTION_NAMES` or the close-label patterns to match — same pattern #37 followed for P&L.)_
- [x] **55.** `qb_w2_summary` — payroll. **Requires QB Payroll subscription**; QBXML payroll endpoints (`PayrollSummaryReportQueryRq`) only return data when subscribed. Document the dependency in the tool description; return a clear error message if no payroll subscription is detected (rather than the raw QBXML status code). _(Closed 2026-05-12. Full end-to-end wire surface for `PayrollSummaryReportQueryRq` shipped: new `buildPayrollSummaryReportRequest` in [src/qbxml/builder.ts](src/qbxml/builder.ts) (conservative schema-order subset — `PayrollSummaryReportType` → `ReportPeriod` → `SummarizeColumnsBy` → `ReportEntityFilter`; no `ReportBasis` because payroll reports are inherently cash); new `runPayrollSummaryReport` manager method via the existing `extractReportData` path; new `handlePayrollSummaryReportQuery` sim handler dispatched before the `endsWith("QueryRq")` catch-all; new `EmployeeWagesTaxesRet` element registered in parser arrayElements. Three Employee seeds with sim-only `PayrollYTDByYear` extension covering 2024 + 2025 across IL / CO / TX (TX exercises the no-state-income-tax branch). Tool `qb_w2_summary` in [src/tools/reports.ts](src/tools/reports.ts) maps per-employee rows onto W-2 box numbers (box1 / box2 / box3-6 / box16 / box17). Two-stage gating: (a) pre-flight edition probe via `session.getHostInfo()` — Pro builds reject with new synthetic statusCode **9003** ("edition does not support QB Payroll") BEFORE any wire round trip; (b) empty-result path returns new synthetic statusCode **9004** ("payroll subscription required or not active") — distinguishes "subscription off" from "no matching employees" via descriptive statusMessage. SSN masked to last 4 (`XXX-XX-1234`) at the sim handler. Cross-year ReportPeriod rejects with 3120. Default `taxYear` resolves to last completed year. 25 new tests in [tests/w2-summary.test.ts](tests/w2-summary.test.ts) + 1 schema-order pin in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) across 4 layers: (1) builder — schema-order pin + no-ReportBasis + ListID-precedence + skip-on-empty; (2) sim handler — three-employee return, TX no-state-fields, EntityFilter scope by ListID/FullName, 2025 year picker, grand totals, empty-scope statusCode 1, cross-year 3120, unsupported PayrollSummaryReportType 3120; (3) tool surface — happy path default + override taxYear, scope by name + listId, 9004 empty-result, 9003 Pro edition gate (via patched `getHostInfo`), PremierAccountant passes the gate, error wrapper with `humanReadable`. Tool count 99 → 100. 742 → 767 tests green. Design rationale in [DECISIONS.md](DECISIONS.md#2026-05-12--qb_w2_summary-ships-full-wire-surface-payrollsummaryreportqueryrq-with-edition-gate--synthetic-90039004-status-codes). **Live verification deferred** — `PayrollSummaryReport`'s row-tree adapter has not been pinned against a real QB Desktop; verified-by-construction structurally. First live exercise will reveal whether `EmployeeWagesTaxesRet` arrives flat (matches sim) or as a row tree under `ReportData` (would need a payroll-specific branch in `adaptLiveReportRet` similar to the SCF route added with #54)._
- [x] **56.** `qb_reconciliation_discrepancy` — surface bank/CC transactions previously marked Cleared but modified since `sinceDate` (the classic "broke a prior reconciliation" signal). _(Closed 2026-05-10. Shipped paired with #56a on shared `CustomDetailReportQueryRq` infrastructure — new `buildCustomDetailReportRequest` helper in [src/qbxml/builder.ts](src/qbxml/builder.ts), `adaptLiveCustomDetailReportRet` + `extractCustomDetailReportData` adapter in [src/qbxml/parser.ts](src/qbxml/parser.ts), `runCustomDetailReport` method in [src/session/manager.ts](src/session/manager.ts), `handleCustomDetailReportQuery` handler in [src/session/simulation-store.ts](src/session/simulation-store.ts). Tool registered in [src/tools/reconciliation.ts](src/tools/reconciliation.ts). Args: `accountName | accountListId`, `sinceDate?` (default 30 days back), `asOfDate?` (default today), `basis?`. Returns `{ account, sinceDate, asOfDate, count, note, candidates }`. Surfaces ONLY the modified-after-cleared signal; postings to QB's "Reconciliation Discrepancies" expense account are reachable via existing `qb_transaction_list_by_account` and explicitly NOT bundled (keeps each tool single-purpose — design rationale in [DECISIONS.md](DECISIONS.md#2026-05-10--customdetailreportqueryrq-infrastructure-for-the-bank-rec-read-side-56--56a-paired)). Read-side; ungated on read-only sessions. Sim handler walks the seven bank-affecting stores with header-account resolution + natural-balance sign convention; modified-date filter slices both inputs to YYYY-MM-DD so toMod includes the whole day. 45 new tests in [tests/reconciliation-read.test.ts](tests/reconciliation-read.test.ts) + 1 schema-order pin in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts). Tool count 80 → 82. 454/454 tests green._
- [x] **56a.** `qb_uncleared_transactions` — read-side companion to the #46 `qb_cleared_status_update` write primitive. _(Closed 2026-05-10. Same infrastructure as #56. Args: `accountName | accountListId` (one required), `asOfDate?` (default today), `clearedStatusFilter?` (`UnclearedOnly` (default = NotCleared+Pending) | `ClearedOnly` | `All`), `basis?`. Returns `{ account, asOfDate, clearedStatusFilter, count, totalAmount, transactions }`. Sign convention on `amount`: positive = increases the account's natural balance (Deposit, CreditCardCharge), negative = decreases (Check, BillPaymentCheck, BillPaymentCreditCard against bank, CreditCardCredit). Transfer hits both From and To accounts independently — emits two rows when both match the filter, with "Transfer in" / "Transfer out" memo to disambiguate. `totalAmount` is the signed sum (matches QB's CustomTxnDetail). Read-side; ungated. Tool count 80 → 82. End-to-end month-end-close workflow now closes through the MCP: discover uncleared → mark cleared → run discrepancy check.)_

## Phase 12 — Remaining missing workflows

- [x] **57.** `qb_invoice_write_off(txnId, writeOffAccount, memo)` — single call. Currently requires create credit memo + `qb_credit_memo_apply` (two calls plus the `editSequence` dance). Internally executes the same two steps in one tool, atomic. _(Closed 2026-05-11. Shipped via the **ReceivePayment + DiscountAmount + DiscountAccountRef** path, not the originally-scoped CreditMemo + apply path — the operator's `writeOffAccount` arg maps directly to `DiscountAccountRef.FullName` (single atomic QBXML write, no compensating delete needed). Same mechanism as QB Desktop's "Discounts and Credits" dialog on the Receive Payments form. Tool reads source invoice → submits a $0 `ReceivePayment` whose `AppliedToTxnAdd` carries `PaymentAmount=0 + DiscountAmount=writeOffAmount + DiscountAccountRef={FullName: writeOffAccount}`. Default `writeOffAmount = source.BalanceRemaining` (full close); explicit `amount` arg supports partial write-off. Validation rejects: unknown source (500), source.BalanceRemaining ≤ 0 ("already paid or closed"), amount > BalanceRemaining, amount ≤ 0. Read-only gate composes via the underlying ReceivePaymentAdd (9001). Idempotency: tool peeks the new `session.peekIdempotencyEntry` cache BEFORE source-read validation — necessary because the prior call's write-off closes the source, so a naive replay would fail the "still open" check before reaching `addEntityIdempotent`'s fingerprint logic. On replay (cache hit + entityType "ReceivePayment"), validation is relaxed; the cached `DiscountAmount` is used as `writeOffAmount` so the rebuilt payload fingerprint-matches the cached entry. **Adjacent sim fix (strict improvement, AR side only):** `applyTxnApplications` + `reverseReceivePaymentApplication` now move `Customer.Balance` by `appliedSum + discountSum` (not just `appliedSum`) — pre-fix, a discount-close left `sum(invoice.BalanceRemaining) ≠ Customer.Balance` for the customer. AP-side `applyBillPayment` has the parallel inconsistency on `Vendor.Balance` (out of scope for #57; flag for a future AP write-off tool). **Adjacent manager fix (strict improvement):** `manager.deleteEntity`'s `isTransaction` list was stale (had `"Payment"` instead of `"ReceivePayment"`, missing CC/Deposit/Transfer/Check etc.) — now matches the canonical list in `buildDeleteRequest` (CLAUDE.md calls these out as needing to stay in sync). 17 new tests in [tests/invoice-write-off.test.ts](tests/invoice-write-off.test.ts) across 6 layers (full happy path + payload surface checks, partial write-off, validation errors, idempotency replay + 9002 conflict, sim regression pins for the AR DiscountAmount/Customer.Balance fix on both forward and reverse paths via `qb_payment_apply` with empty applyTo, read-only gate compose). 596/596 tests green (was 579 + 17). Tool count 91 → 92. Design rationale in [DECISIONS.md](DECISIONS.md#2026-05-11--qb_invoice_write_off-ships-via-receivepayment--discount-not-creditmemo--apply).)_
- [x] **57a.** `qb_invoice_duplicate(sourceTxnId, overrides)` — workflow stand-in for the operator's monthly retainer billing pain that #45 was originally meant to solve. Reads the source invoice's lines via `qb_invoice_list { txnId, includeLineItems: true }`, submits a fresh `InvoiceAddRq` with the carried `CustomerRef` + `lines` and a new `TxnDate` / `RefNumber` (operator-supplied overrides). _(Closed 2026-05-10. Registered in [src/tools/invoices.ts](src/tools/invoices.ts) as a composite tool — no new wire types, no new manager methods. Reads source via `session.queryEntity("Invoice", { TxnID, IncludeLineItems: true })`, builds fresh InvoiceAdd from CustomerRef + InvoiceLineRet → InvoiceLineAdd mapping (drops TxnLineID — new invoice generates its own), carries optional ClassRef/TermsRef/SalesRepRef/PORefNumber header refs, applies operator overrides for txnDate/dueDate/refNumber/memo/customerName/customerListId. Default carry policy: TxnDate → today (NOT carried — duplicating to the same date is rarely what you want), DueDate → unset (it's relative to TxnDate; carrying makes no sense for a different date), RefNumber → unset (avoids collisions on monthly retainer flows; operator supplies or QB autonumbers), Memo → "Duplicate of <source ref or TxnID>". Idempotency-keyed via `addEntityIdempotent`. Read-only gate composes (9001 from the InvoiceAdd half). Unknown sourceTxnId returns structured 500. Header-only sources allowed. 14 new tests in [tests/invoice-duplicate.test.ts](tests/invoice-duplicate.test.ts) cover happy path (single + multi-line, customer retarget by name and ListID, ClassRef/TermsRef carry, default Memo + Memo fallback, non-carry of TxnDate/DueDate/RefNumber, override-wins, header-only source, customer-balance composition with handleAdd), error paths (unknown TxnID 500, read-only 9001), and idempotency (same-key same-source replay + different-source 9002 conflict). Differs from `qb_estimate_convert_to_invoice` in carry policy (convert carries RefNumber from source; duplicate doesn't — different use case). Tool count 79 → 80. Build clean, 409/409 tests green (was 395 + 14 new). **2026-05-11 follow-up: pattern mirrored to bill / sales-receipt / journal-entry as a quick-wins batch.** `qb_bill_duplicate` ([src/tools/bills.ts](src/tools/bills.ts)) — AP analog; carries VendorRef + ExpenseLineRet → ExpenseLineAdd + ItemLineRet → ItemLineAdd; same non-carry policy on TxnDate/DueDate/RefNumber + default Memo; retarget via vendorName/vendorListId. `qb_sales_receipt_duplicate` ([src/tools/sales-receipts.ts](src/tools/sales-receipts.ts)) — cash-sale analog; additionally carries PaymentMethodRef + DepositToAccountRef from source (overridable via paymentMethodName/depositToAccountName); same non-carry policy on TxnDate/RefNumber + default Memo; retarget via customerName/customerListId. `qb_journal_entry_duplicate` ([src/tools/journal-entries.ts](src/tools/journal-entries.ts)) — GL-only analog; carries BOTH line sides verbatim (JournalDebitLineRet → JournalDebitLineAdd + JournalCreditLineRet → JournalCreditLineAdd) which preserves the sum(debits)===sum(credits) invariant by construction; carries per-line EntityRef + ClassRef; carries IsAdjustment flag (overridable); no entity-retarget arg at the tool level (per-line EntityRef updates are qb_journal_entry_update territory). All three are pure composites over existing query + add primitives — no new wire types, no manager changes. Each uses `addEntityIdempotent` for replay support. Read-only gate composes (9001 from the *AddRq half). 38 new tests across [tests/bill-duplicate.test.ts](tests/bill-duplicate.test.ts) (13), [tests/sales-receipt-duplicate.test.ts](tests/sales-receipt-duplicate.test.ts) (12), [tests/journal-entry-duplicate.test.ts](tests/journal-entry-duplicate.test.ts) (13) covering happy path / carry-vs-non-carry policy / overrides / retargets / error paths (500 unknown source + 9001 read-only) / idempotency (same-key replay + 9002 conflict on different source). [src/index.ts](src/index.ts) instructions block extended on all three categories + idempotency-keyed tools list. [README.md](README.md) tool count 92 → 95, architecture diagram + bill/SR/JE tables updated. Tool count 92 → 95. 634/634 tests green (was 596 + 38).)_
- [x] **58.** `qb_invoice_batch_create({ invoices: [...] })` and `qb_sales_receipt_batch_create({ receipts: [...] })` — atomic, all-or-nothing. Same envelope pattern as item 43 (batch JE). Replaces the Excel intermediate as a transport layer for high-volume entry. _(Closed 2026-05-11. Both tools shipped riding on the multi-request envelope infrastructure #43 generalized — pure composites over `session.executeBatchAdd` / `executeBatchAddIdempotent` (entity-type-generic since #43) + `session.deleteEntity` for compensating rollback. No new wire types, no manager methods, no parser changes, no sim-store changes. Each entry takes the same shape as the underlying `_create` tool (`qb_invoice_create` / `qb_sales_receipt_create`); upfront customer-ref validation rejects the WHOLE batch with statusCode 3120 BEFORE any wire I/O on missing customerName/customerListId. Wire envelope uses `onError="stopOnError"`; mid-wire failures trigger automatic compensating delete of any prior-posted entries in REVERSE post order (so the most-recent posting goes first — minimizes the chance that QB's cascading bookkeeping invalidates an earlier delete). Per-entry status enum: `posted` (committed), `rolled-back` (was posted, auto-deleted), `orphaned` (was posted but rollback delete itself failed — operator cleans up manually using `qb_invoice_delete` / `qb_sales_receipt_delete` with the surfaced TxnID), `failed` (wire-side rejection), `skipped` (post-stopOnError). On full success: `success: true + count + invoices/salesReceipts: [...]`. On partial failure: `success: false + atomic: true + rolledBack: bool + failedAt + failedReason + summary + invoices/salesReceipts: [...]` with `isError: true`. Idempotency at the BATCH level: optional `idempotencyKey` fingerprints the entire entries list (reorder/add/remove changes the hash and surfaces 9002 — use a fresh key). Only fully-successful batches are cached (carried over from `executeBatchAddIdempotent`'s contract); upfront-validation rejections are NOT cached (allows the operator to retry the same key with a corrected payload). **Customer.Balance composition is the only structural difference between the two tools:** invoice batches move AR via `handleAdd`/`handleTxnDel`; SR batches don't (cash sales never moved AR — pure record removal on rollback). 26 new tests across 2 files — 14 in [tests/invoice-batch.test.ts](tests/invoice-batch.test.ts), 12 in [tests/sales-receipt-batch.test.ts](tests/sales-receipt-batch.test.ts). Coverage: full-success multi-entry + single-entry, header-only entry support, Customer.Balance composition (AR-moves for invoice; AR-unchanged for SR), upfront customer-ref validation, schema bounds (≥1, ≤100, exactly-100), compensating rollback on mid-batch failure (mocks `executeBatchAdd` to inject a wire-side posted+failed+skipped mix, verifies REVERSE-order deleteEntity dispatch), orphaned status when rollback delete itself fails, idempotency (full-success replay, validation-rejection-not-cached, different-payload-same-key 9002 conflict). [src/index.ts](src/index.ts) instructions block: invoice + SR category lines extended with the new tool descriptions; idempotency-keyed tools list now includes both new tools with a generalized "batch tools" callout. [README.md](README.md) header `95 total` → `97 total`, architecture diagram updated, invoice/SR tables extended. Tool count 95 → 97. 634 → 660 tests green. No schema-order risk to verify on Windows — both tools reuse InvoiceAdd / SalesReceiptAdd request bodies already pinned by existing tests/builder-emit-order.test.ts assertions; the multi-request envelope structure is the same shape JE batch already validated live.)_
- [x] **59.** Attachments — attach a source PDF (vendor bill, deposit slip) to a transaction. QBXML: `AttachableAdd` against `AttachableRef`. For audit-trail use cases. Scope: tool that takes a `txnId` + local file path, uploads via the Attachable API, returns the attachment's `ListID`. _(Closed 2026-05-12. Three tools shipped in new [src/tools/attachments.ts](src/tools/attachments.ts): `qb_attachment_add` / `qb_attachment_list` / `qb_attachment_delete` wired via new `registerAttachmentTools` in [src/index.ts](src/index.ts). New `AttachableRet` registered in parser arrayElements. New `handleAttachableAdd` in [src/session/simulation-store.ts](src/session/simulation-store.ts) dispatched before the `endsWith("AddRq")` catch-all — validates `FileReference.FullPath` exists on disk via `fs.statSync`, validates `ObjectRef.{TxnID|ListID}` target exists by walking every non-Attachable store, derives `FileName` / `FileSize` / `FileExtension` from disk + reverse-looks-up `ObjectType` / `FullName` from the target entity, defaults `AttachmentType: "Normal"`, normalizes `ShowAsImage` from string/bool. New `ObjectFilter` branch in the GENERIC `handleQuery` (entity-type-agnostic — scoped by `ObjectRef.{TxnID|ListID}`) for AttachableQueryRq scoping. Delete rides the existing generic `handleListDel` path (Attachable is a list entity, not a transaction). Tool layer enforces: (a) absolute-path required (relative paths reject with 3120 upfront — a relative path resolved against QB Desktop's CWD is rarely intended); (b) mutual-exclusivity on `txnId` / `listId` for add (both supplied or neither rejects with 3120); (c) at-most-one filter on list (txnId / targetListId / attachableListId). Read-only gate composes through `addEntity` / `deleteEntity`'s `assertWritable` (9001). Idempotency via `addEntityIdempotent` (9002 on key conflict). No new manager methods — `addEntity` / `queryEntity` / `deleteEntity` cover all three tool surfaces. 31 new tests in [tests/attachments.test.ts](tests/attachments.test.ts) across 6 layers: (1) sim handleAttachableAdd — Invoice + Customer + Bill happy paths, file-not-found 500, missing FileReference 3120, missing ObjectRef 3120, ObjectRef without TxnID/ListID 3120, unknown target 500; (2) ObjectFilter — by TxnID, by ListID, empty result, unfiltered all-return; (3) sim delete — happy path removes, unknown ListID 500; (4) qb_attachment_add tool — txnId + listId happy paths, mutual-exclusivity (both / neither), relative-path rejection, idempotency replay + 9002 conflict, read-only gate 9001, file-not-found surfacing with humanReadable; (5) qb_attachment_list tool — all-unfiltered, txnId scope, targetListId scope, attachableListId fetch, multi-filter rejection 3120; (6) qb_attachment_delete tool — happy path, unknown ID 500, read-only gate 9001. Tests use `mkdtempSync` + `writeFileSync` for stub files; cleanup in afterEach. Tool count 100 → 103. 767 → 798 tests green. Design rationale in [DECISIONS.md](DECISIONS.md#2026-05-12--qb_attachment_-ships-full-attachable-surface-with-file-path-validation-at-sim-handler--tool-layer). **Live verification deferred** — Attached Documents support is edition-dependent in real QB (the SDK reference doesn't fully enumerate which editions); first live exercise will reveal whether `AttachableAdd` succeeds or returns a QB-side "feature not enabled" error. Wire shape verified-by-construction structurally; the `AttachableRet` payload may carry additional fields beyond the seven sim emits (e.g. `Thumbnail`, `OriginalRequestID`) — extending `handleAttachableAdd` to propagate is trivial.)_

## Phase 13 — Data model gaps in returned objects

Beyond item 41 (line-level detail), these fields are currently missing or hard to reach.

- [ ] **60.** Customer email / phone in invoice list payload — currently to send a collection email you need a second `qb_customer_list` call per customer. Add `includeCustomerContact: boolean` arg, or join `Email`/`Phone` server-side from the customer cache when populated.
- [ ] **61.** Custom-field (DataExt) support across all entities. Most firms use them heavily (engagement type, tax year, partner assigned). Currently invisible. QBXML: `OwnerID` + `DataExtRet` round-trip. Add `includeCustomFields: boolean` and a separate `qb_custom_field_list` to discover what's defined per entity.
- [ ] **62.** Sub-customer / job hierarchy helpers. `CustomerRef.FullName` already encodes "Customer:Job" syntax but no traverse parent/child or filter-by-job helper exists. Add `qb_customer_jobs(parentListId)` and a `parentListID` / `jobOnly` filter on `qb_customer_list`.
- [ ] **63.** Memo / note full-text search across transactions — currently no way to search by memo content. QBXML doesn't support memo-substring filtering server-side, so wire as a server-side filter applied to `*Ret.Memo` after the QBXML response (with cost noted in the tool description: pulls all matching txns, then filters in-process).

## Phase 14 — Safety and DX

- [ ] **64.** Dry-run mode — `dryRun: true` on every `*_create` / `*_update` / `*_delete`. Returns "what would happen" with the validated payload (built QBXML envelope + simulation-store preview of resulting state) but does not commit. Today the only way to test a mutation is to do it.
- [ ] **65.** Better error surfaces. Today `statusCode -1` "QuickBooks found an error when parsing the provided XML text stream" is unactionable. Pass through the full QBXMLRP2 response, plus heuristic field hints — detect `"missing element: X"` / `"invalid argument: X"` / `"out of order: X"` patterns in the QB error message and surface the offending field name + the canonical schema-order for the parent element. Anchor on the `tests/builder-emit-order.test.ts` canonical sequences as the source of truth.
- [ ] **66.** `qb_audit_log({ txnId })` and `qb_audit_log({ dateRange })` — QB tracks who changed what; expose it. **Note:** QBXML SDK exposes audit-log read via `TxnReportQueryRq` (with audit-trail mode) only on Enterprise editions; Pro/Premier do not. Document the edition dependency; return a structured error on unsupported editions.
- [x] **67.** `qb_session_status` — health check / probe. Verify SDK connection without performing a real query. Useful for orchestration retries. Returns `{ connected, mode, companyFile, qbxmlVersion, lastError, version }`. _(Closed 2026-05-13. New tool registered in [src/tools/reports.ts](src/tools/reports.ts) next to `qb_session_connect` / `qb_session_disconnect`. Default path is zero wire I/O — returns `connected` / `mode` / `companyFile` / `appName` / `appId` / `qbxmlVersion` / `readOnly` / `ticket` / `openedAt` / `serverVersion` / cached `hostInfo` (peek-only, never triggers a fetch) / `retryStats` (`lastTransientRetryAt` / `transientRetryCountLastHour` / `totalTransientRetries`). Opt-in `probe: true` runs `HostQueryRq` with `refresh: true` (lightest available real call) — fail-soft: a probe failure surfaces as `probe.ok: false` WITHOUT making the response `isError`. Opt-in `includeClosingDate: true` folds `PreferencesQueryRq` into the response under `closingDate` (same shape as `qb_closing_date_get`, same fail-soft contract). Retry observability added to `QBSessionManager`: `transientRetryTimestamps` array + `totalTransientRetries` counter pushed in `sendLiveRequestWithRetry` (the existing #84 retry path), exposed via new `getTransientRetryStats()` which prunes to the last hour on read. New `getAppName()` / `getAppId()` / `getQbxmlVersion()` config getters mirror the existing `getCompanyFile()` pattern. `switchCompanyFile` now also clears `transientRetryTimestamps` + `totalTransientRetries` (a fresh book is a fresh observability window) alongside the existing `hostInfoCache` clear. 19 new tests in [tests/session-status.test.ts](tests/session-status.test.ts) covering the rolling-window prune math, the default snapshot shape with zero wire I/O verified, the readOnly toggle, the hostInfo cache surfacing after a getHostInfo call, the probe happy + sad paths (sad surfaces probe.ok:false WITHOUT making outer isError), includeClosingDate happy + sad paths, retry stats surfacing through the tool, and switchCompanyFile clearing both retry stats and hostInfo cache. 876 → 895 tests green. README tool count 105 → 106; new entry in Session Management table.)_

## Phase 15 — Practice-specific wins (operator's firm + skills)

These directly upgrade the skills the operator has already built. Some are also product requirements that should land in [REQUIREMENTS.md](REQUIREMENTS.md) once scoped.

- [x] **68.** `qb_trial_balance_export(asOfDate, basis)` — return TB in the exact column shape consumed by the operator's `trial-balance-workup` skill. Today the skill consumes a CSV exported from QB Desktop manually. Cutting that turns a manual hand-off into one tool call. Coordinate column names with the skill's parser before shipping. _(Closed 2026-05-13. New tool registered in [src/tools/reports.ts](src/tools/reports.ts) right after `qb_balance_summary`. Composite of 5 wire calls in the default path: `AccountQueryRq` + `BalanceSheetStandard` + `ProfitAndLossStandard` + `InvoiceQueryRq` (for AR aging total) + `BillQueryRq` (for AP aging total). Returns one row per posting account with non-zero balance — `accountListId` / `accountName` / `accountNumber` / `accountType` / `taxLine` (from `Account.TaxLineInfoRet.TaxLineName` — populated by live QB; empty in sim until #69 seeds it) / `debitBalance` / `creditBalance` / `isActive` / `lastActivityDate`. **Debits and credits split by natural-balance side** via two `Set<string>` (NATURAL_DEBIT_TYPES / NATURAL_CREDIT_TYPES): Asset/Expense balances → debit column, Liability/Equity/Income → credit column. **Contra-balances flip column** (e.g. negative AR = customer credit on file emits as a Credit column entry, NOT as a negative debit) — matches the workpaper convention CPAs expect. Sort: canonical AccountType (ASSET → LIAB → EQ → INCOME → EXPENSE) → AccountNumber (numbered accounts first within type, lex-compared since AccountNumber is a string in QB) → alphabetical FullName. **NonPosting accounts excluded by TB convention** (use `qb_balance_summary` for those). **Plus four reconciliation cross-checks** with cent-tolerance match boolean + computed delta: (a) `balanceSheet` — Assets ≡ Liab+Equity from `bsRet.Totals.{TotalAssets, TotalLiabilities, TotalEquity}`; (b) `netIncome` — `bsRet.Totals.NetIncome` ≡ `pnlRet.Totals.NetIncome` (period income closes into equity correctly); (c) `arReconciliation` — TB AR account total ≡ AR aging total; (d) `apReconciliation` — TB AP account total ≡ AP aging total. **Any mismatch is an audit signal** — the workpaper workflow surfaces it loudly. **Defaults:** `includeInactive: false` (inactive accounts dropped), `includeZeroBalances: false` (zero-balance rows dropped — TB convention), `includeLastActivityDate: false` (opt-in; triggers N+5 round trip fanout via `session.queryTransactions` per account to surface most-recent posting date — ~10s for a 200-account chart in live mode; per-account errors land in `warnings` and the row's `lastActivityDate` stays null). All math in pure `buildTrialBalance(accounts, bsRet, pnlRet, arAgingTotal, apAgingTotal, options)` helper exported for unit testing. **Bridges the operator's `trial-balance-workup` skill** from manual CSV-export-from-QB-Desktop to direct MCP query. **Prompt update:** `/trial_balance_workup` collapsed from an 8-tool recipe (qb_balance_summary + qb_account_list + qb_tax_line_mapping + qb_balance_sheet_report + qb_pnl_report + qb_ar_aging + qb_ap_aging + qb_transaction_list_by_account) to a single `qb_trial_balance_export({ includeLastActivityDate: true })` call — drill-down tools stay referenced for when a cross-check fires. **Sim caveat documented loudly:** sim's BS reads `Account.Balance` for AS/LI/EQ snapshot (`asOfDate` advisory for those buckets); the P&L walk IS date-bounded in both modes; cross-check arithmetic is correct in both. **Fresh sim seed has deliberate subledger drift** (AR Account.Balance=26700 but open invoices walk to 16000; AP Account.Balance=3700 but every seeded bill is IsPaid:true → walks to 0) — the tool correctly surfaces these as cross-check deltas; pinned by e2e tests that assert the cross-check FIRES (not that the seed reconciles). 26 new tests in [tests/trial-balance-export.test.ts](tests/trial-balance-export.test.ts) across 2 layers: (1) `buildTrialBalance` unit tests — 17 covering natural-balance side, contra-balance column-flip, NonPosting exclusion, zero/inactive filtering, BS synthetic row drop (Net Income + Balancing Adjustment), TaxLine surface, canonical sort, totals balance, 4 cross-checks under reconciling AND broken scenarios, cent-tolerance threshold, lastActivityDate map, empty BS/PnL edge; (2) e2e via QBSessionManager — 9 covering wire orchestration, Checking debit column, AP credit column (P&L empty in sim so AP is the natural-credit account guaranteed present), NonPosting exclusion, AR/AP drift surfacing, balanceSheet reconcile via Balancing Adjustment closure, netIncome match (both 0). [src/index.ts](src/index.ts) instructions block extended on the reports line. [src/prompts/workflows.ts](src/prompts/workflows.ts) `/trial_balance_workup` rewritten to call the new one-shot tool; [tests/mcp-prompts.test.ts](tests/mcp-prompts.test.ts) updated to assert the new tool reference set. [README.md](README.md) tool count 106 → 107 + new row in Reports & Queries table + architecture diagram updated. 895 → 921 tests green. Tool count 106 → 107.)_
- [x] **69.** `qb_tax_line_mapping(accountListId?)` — return QB tax-line assignment (1120-S / 1065 / Sch C / Sch E / etc.) for any income/expense account. Bridge from books to tax software; currently rebuilt manually every workpaper. QBXML: `AccountQueryRq` already returns `TaxLineInfoRet`; just expose it. _(Closed 2026-05-14. New tool registered in [src/tools/reports.ts](src/tools/reports.ts) right after `qb_trial_balance_export`. Pure read-side composite over `session.queryEntity("Account", filters)` — no new wire types, no manager method, no parser changes (`AccountQueryRq` + `AccountRet` were already covered). Args: `accountListId?` / `accountName?` / `accountType?` / `includeInactive?` (default false) / `includeUnmapped?` (default false). Returns `{ count, mappedCount, unmappedCount, accounts: [{accountListId, accountName, accountNumber, accountType, isActive, taxLineId, taxLineName, isUnmapped}] }`. Pure helper `buildTaxLineMapping(accounts, options)` exported for unit testing — handles the projection + sort + mapped/unmapped split. **Mapped definition:** `TaxLineInfoRet.TaxLineName` is a non-empty string. An account with `TaxLineID` alone (no name) is treated as `isUnmapped: true` (the name is the workpaper-readable label every consumer keys on) but `taxLineId` is still preserved on the row as an audit signal. **Sort:** canonical TB_ACCOUNT_TYPES order → AccountNumber (lex compare since AccountNumber is a string in QB — `1000-1` / `1000.A` patterns) → alphabetical FullName; numbered accounts sort before unnumbered within type; unknown AccountTypes sort last alphabetically. **Sim improvement (paired with the tool):** added representative `TaxLineInfoRet` (TaxLineID + TaxLineName) to 8 of the 10 seeded accounts in [src/session/simulation-store.ts](src/session/simulation-store.ts) `seedData()` — using QB Desktop's standard tax-line naming convention (B/S-Assets / B/S-Liabs/Eq. / Income / COGS / Deductions). `Savings` + `Consulting Revenue` intentionally left UNMAPPED so the `includeUnmapped` filter has something to exercise against fresh seed. **Sim improvement (strict, paired with the tool):** `handleQuery` now applies the `AccountType` filter (pre-#69 it silently ignored AccountType, so `qb_account_list({accountType: "Income"})` returned every account in sim — the new tool needs the filter to work, and applying it is a strict improvement on every other caller that already passed AccountType through; no existing tests passed AccountType so nothing breaks). **Adjacent fix:** `qb_trial_balance_export`'s `taxLine` column now surfaces non-null values for the seeded mapped accounts in sim (was always null pre-#69 because the seed didn't carry `TaxLineInfoRet`); pinned in the new tax-line-mapping integration test. The trial-balance description in [src/tools/reports.ts](src/tools/reports.ts), [src/index.ts](src/index.ts), and [README.md](README.md) updated to reflect that sim populates `taxLine` now too. 28 new tests in [tests/tax-line-mapping.test.ts](tests/tax-line-mapping.test.ts) across 4 layers: (1) `buildTaxLineMapping` unit — 12 covering default-mapped-only, includeUnmapped:true, taxLineId/taxLineName surfacing, TaxLineID-only treated as unmapped, empty-name treated as unmapped, count math, mapped/unmapped split, canonical sort with numbered-before-unnumbered + unknown-AccountType-last, IsActive preservation, empty input, missing FullName/Name skip; (2) e2e via QBSessionManager — 8 covering 8-mapped/2-unmapped seed shape, includeUnmapped:true full count, Checking row carries seeded TaxLineInfoRet end-to-end, AccountType filter scopes the wire query (handleQuery #69 strict improvement), Income scope mapped vs includeUnmapped, ListID + FullName scopes, sort order against fresh seed; (3) tool surface via fake McpServer harness — 7 covering canonical envelope shape, includeUnmapped split, accountType filter, accountListId scope, accountName scope, empty-result-as-success path, error wrapping with humanReadable; (4) integration with #68 — 1 pinning that `qb_trial_balance_export` now surfaces `taxLine` from sim seed (via `buildTrialBalance` against a fresh sim session). 921 → 949 tests green. Tool count 107 → 108. README tool count + architecture diagram updated. **Live verification deferred** — sim seed values are deterministic; live exercise against `VR Tax & Consulting Inc..qbw` will reveal real QB's actual TaxLineName text (the tool just surfaces whatever Account.TaxLineInfoRet.TaxLineName carries, no parsing or rewriting at the tool layer, so live should "just work" structurally). First live run should also confirm the chart's tax-line coverage (mappedCount / unmappedCount split is itself a workpaper-prep signal — every income/expense account should be mapped before filing).)_
- [x] **70.** `qb_engagement_profitability(customerListId, dateRange)` — pulls revenue + time + reimbursable expenses for a job. Closer to ClosePilot's surface than the raw aging report. Composite that combines invoice/SR + time-tracking + expense-line queries. _(Closed 2026-05-15. New tool registered in [src/tools/engagement-profitability.ts](src/tools/engagement-profitability.ts) via `registerEngagementProfitabilityTools` wired into [src/index.ts](src/index.ts). Pure composite over existing session primitives — `queryEntity` for each of Customer / Invoice / SalesReceipt / CreditMemo / TimeTracking / Bill / Check / CreditCardCharge. No new wire types, no parser changes, no manager methods. Reuses `parseDurationToHours` from [src/tools/time-tracking.ts](src/tools/time-tracking.ts) — first cross-tool consumer of the helper, unblocked by #78. **API:** `customerListId` | `customerName` REQUIRED (the engagement IS the customer; no match → 3120), `fromDate` / `toDate` both REQUIRED — engagements have explicit windows, no defaults — and validated `fromDate <= toDate`. Three section toggles (`includeRevenue` / `includeTime` / `includeReimbursableExpenses`) all default true; `basis` defaults Accrual. **Three fail-soft sections:** (1) Revenue — Invoice + SalesReceipt header totals MINUS CreditMemo header totals over the window, scoped server-side via `EntityFilter: {ListID}`. Each row emits as positive `amount`; netRevenue is the computed delta. (2) Time — `TimeTracking` entries with `TxnDateRangeFilter` server-side then POST-FILTERED by `CustomerRef` match (QB's `TimeTrackingQueryRq` has no `CustomerFilter` at any version — same contract `qb_time_track_list` documents). Hours derived from `Duration` via `parseDurationToHours`; rolled up `byWorker` (EntityRef = the worker) + `byServiceItem` plus the per-entry `entries` array. Billable vs non-billable split. (3) Reimbursable expenses — Bill / Check / CreditCardCharge with `IncludeLineItems: true` (REQUIRED — sim + real QB strip *LineRet without it; missing this flag silently returns zero lines), then walk `ExpenseLineRet` + `ItemLineRet` line-by-line and keep only those whose LINE-LEVEL `CustomerRef` matches (Bill/Check/CCC headers don't carry customer scope — job-costing tags live on each line). Total = sum of matched line amounts (NOT parent txn totals — one bill might split across multiple jobs and only a subset of its lines tag THIS customer). **Derived summary** (`revenue` / `reimbursableExpenseCost` / `grossProfit` / `marginPct` / `billableHours` / `totalHours` / `revenuePerHour` / `billableRate`) is OMITTED when any section is `'error'` OR any toggled off — partial summary would silently misreport profitability. `marginPct` and `revenuePerHour` / `billableRate` are `null` (not 0) when their denominator is zero. **Customer lookup is the one non-fail-soft path** — unknown customer / `CustomerQueryRq` failure fails the whole tool with `'pre-flight failed:'` prefix. **Output:** `{ success, customer: {listId, fullName, balance?}, fromDate, toDate, basis, generatedAt, sections: {revenue?, time?, reimbursableExpenses?}, sectionStatus: {revenue, time, reimbursableExpenses}, summary? }`. 33 new tests in [tests/engagement-profitability.test.ts](tests/engagement-profitability.test.ts): registration, required args (missing customer → 3120, date inversion → 3120, unknown customer → 3120, pre-flight failure), happy path (customer resolves via listId + name; sectionStatus shape), revenue section (seed Acme $7500 invoice; Global isolation; out-of-window empty; CreditMemo subtracts to netRevenue; SalesReceipt contributes), time section (14.5h Acme totals; byWorker / byServiceItem rollups; Bob's no-customer entry doesn't leak; Global's 4.25h entry; non-billable contributes to totalHours but not billableHours), reimbursable expenses (zero against fresh seed; single Bill line tagged Acme; multi-line Bill where only Acme line counts; Check via PayeeEntityRef; non-billable line bucketed correctly), summary block (full shape; null `revenuePerHour` on zero hours; null `marginPct` on zero revenue; summary omitted on toggle-off), section toggles (revenue off / time off / all three off), fail-soft (synthetic TimeTracking failure → time only errors; summary omitted when any section is error). **Key wire-level gotcha discovered + pinned:** `IncludeLineItems: true` is REQUIRED on Bill/Check/CreditCardCharge queries — both sim and real QB strip `*LineRet` keys without it. Initial implementation omitted the flag and every reimbursable-expense test failed (zero lines found). 1083 → 1116 tests green. Tool count 122 → 123. **Live verification deferred** — sim-verified end-to-end; first live exercise should run `qb_engagement_profitability({ customerName: "<an active client>", fromDate: "...", toDate: "..." })` against `VR Tax & Consulting Inc..qbw` and confirm (1) revenue section captures real invoices, (2) time section returns hours for any TimeTracking entries against that customer, (3) reimbursable expenses surfaces any job-costed Bill/Check expense lines, (4) summary block reconciles. README sync deferred (still at 120 from carried #71/#75/#78).)_
- [x] **71.** `qb_client_packet(customerListId, taxYear)` — bundles TB + GL + bank rec + fixed asset detail + payroll summary as one call. The workflow run 2,000 times per tax season. Composite that calls items 53 (GL), 68 (TB), 56 (bank rec discrepancy), payroll summary, fixed-asset list. _(Closed 2026-05-14. New tool registered in [src/tools/client-packet.ts](src/tools/client-packet.ts) via `registerClientPacketTools` wired into [src/index.ts](src/index.ts). Pure composite over existing session primitives — `queryEntity` / `queryTransactions` / `runReport` / `runCustomDetailReport` / `runPayrollSummaryReport` / `getHostInfo`. No new wire types, no parser changes, no manager methods. Reuses `buildTrialBalance` + `buildGeneralLedgerSection` directly from [src/tools/reports.ts](src/tools/reports.ts); bank-rec row formatting + W-2 box mapping inlined (small, decoupling the composite from the surface shape of two other tools intentionally). **API:** `taxYear` required (date window is always Jan 1 → Dec 31), `customerListId` / `customerName` optional (lookup surfaces FullName + Balance as packet header — does NOT filter the underlying reports; the .qbw file IS the client), `basis` default Accrual, five section toggles (`includeTrialBalance` / `includeGeneralLedger` / `includeBankReconDiscrepancy` / `includePayrollSummary` / `includeFixedAssetDetail` all default true), `glScope: 'PnLOnly' | 'AllAccounts'` (default `PnLOnly` — Income/Expense/COGS/OtherIncome/OtherExpense), `bankReconDiscrepancySinceDate` (default = start of taxYear). **Fail-soft per section** — same contract `qb_session_status` established. A single section's failure lands in `sections.<name>.error` with `sectionStatus.<name>` flipping to `'error'`; the rest of the packet still returns. Only `AccountQueryRq` failure fails the whole tool (no chart of accounts → no section can build). Payroll has THREE skip states: edition === Pro → `skipped(9003)`, wire returns zero rows → `skipped(9004)`, probe itself fails → `error`. Bank rec discrepancy fans out across every Bank + CreditCard account; per-account errors land INSIDE that account's entry (not as warnings) so siblings stay clean. **Fixed Asset detail** is a new surface — no existing tool exposes it. Pulls every active `FixedAsset` account, fans out `queryTransactions` for the tax-year window, returns per-account current `Balance` + `openingBalance` / `closingBalance` / `periodChange` + every posting (Form 4562 input). Empty `accounts` array against fresh seed is NOT an error condition. **Output:** `{ success, taxYear, fromDate, toDate, basis, generatedAt, customer | null, sections: {...}, sectionStatus: {...}, warnings? }`. 23 new tests in [tests/client-packet.test.ts](tests/client-packet.test.ts): registration, full default packet shape (TB rowCount + crossChecks, GL scope + accountCount, recon perAccount fanout, payroll W-2 box mapping, FA empty-on-seed + populated-after-seed), section toggles (TB only, all-off, payroll skip-probe), customer context (listId lookup, name lookup, unknown-customer warning), fail-soft contract (synthetic payroll wire failure → only payroll fails; AccountQueryRq failure → whole tool fails; bank rec per-account error → siblings unaffected), Pro edition gate (skipped 9003), basis Cash pass-through. 1014 → 1037 tests green. Tool count 120 → 121. **Live verification deferred** — sim-verified end-to-end, but the underlying live wire paths for TB / GL / recon / W-2 / fixed-asset queries haven't been live-pinned against a real QB Desktop. First live exercise should run `qb_client_packet({ taxYear: <last completed FY>, customerListId: <one tax-prep client>, customerName: "..." })` against `VR Tax & Consulting Inc..qbw` and confirm: (1) every section emits — TB cross-checks reconcile, GL `accountCount` matches active P&L accounts, bank rec returns ≥1 perAccount entry per bank/CC account on file, payroll returns W-2 boxes for any employee with YTD activity, FA returns ≥1 account if the chart has Fixed Asset accounts; (2) `sectionStatus` is `'ok'` for every non-skipped section; (3) overall `generatedAt` round-trips ISO 8601 cleanly. The composite layers six different *QueryRq wire types — any schema-order class of bug from any of them (the pattern that bit #37 with `GeneralSummaryReportQueryRq`) will surface here.)_

## Phase 16 — Architectural improvements

- [ ] **72.** Generic `qb_transaction_list({ types: [...], filters })` — replaces half the per-type list tools. "Show me everything for customer X in March" is currently 6 calls (invoice, SR, payment, CM, JE, statement charge); should be 1. Wire as a single `TransactionQueryRq` with the requested types under `IncludeAll`. Keep per-type tools as thin wrappers for compatibility.
- [ ] **73.** Streaming responses for large lists — a 2,000-customer dump should not require N pagination round trips when QB can produce one stream. MCP stdio supports response streaming via incremental notifications; investigate whether the MCP SDK's `server.tool` registration model supports it, or whether these tools need to drop below the helper.
- [ ] **74.** MCP-side caching of stable lookups (chart of accounts, customer list, item list, terms list, class list) with explicit invalidation. These barely change but currently cost a round trip every call. Add `useCache: boolean` (default `true`) and `qb_cache_invalidate({ entity })`. Cache lives in `QBSessionManager`, scoped to the current `companyFile`, cleared on `qb_company_open`.

## Phase 17 — Domain coverage gaps (entire areas missing today)

[CLAUDE.md](CLAUDE.md) lists `Deposit`, `Transfer`, `Check`, and `SalesOrder` as tracked transaction entities, but `src/tools/` has no banking.ts / sales-orders.ts / sales-tax.ts / time.ts / inventory.ts. These are entire workflow surfaces the MCP can't touch today. None block items 37-39 (P0 bugs) but each blocks a real practice workflow once core bugs are cleared.

- [x] **75.** Banking primitives — `qb_deposit_create` / `qb_deposit_list` / `qb_deposit_update` / `qb_deposit_delete` (`DepositAddRq` / `DepositQueryRq` / `DepositModRq` / `TxnDelRq`); same CRUD for `Transfer` (`TransferInventoryAddRq` for inventory, plain `TransferAddRq` for cash) and `Check` (`CheckAddRq` covers vendor checks AND non-A/P bank disbursements). Without these, ad-hoc cash movement and bank deposits are invisible to the MCP — which means month-end bank rec (Phase 10 item 46) has no way to see deposits and transfers it's reconciling against. _(Closed 2026-05-14. 12 new tools shipped across 3 new tool files — [src/tools/deposits.ts](src/tools/deposits.ts), [src/tools/checks.ts](src/tools/checks.ts), [src/tools/transfers.ts](src/tools/transfers.ts) — each registered in [src/index.ts](src/index.ts) via `registerDepositTools` / `registerCheckTools` / `registerTransferTools`. Pure composites over the existing generic `addEntity` / `modifyEntity` / `deleteEntity` / `queryEntity` / `addEntityIdempotent` primitives — no new wire request types, no new manager methods. The whole infrastructure was already in place (`Deposit`/`Transfer`/`Check` were in `isTransactionType`, `isTransaction` lists in [src/qbxml/builder.ts](src/qbxml/builder.ts) and [src/session/manager.ts](src/session/manager.ts), `BANK_AFFECTING_TXN_TYPES`, parser `arrayElements` for `DepositRet` / `DepositLineRet` / `TransferRet` / `CheckRet` / `ExpenseLineRet` / `ItemLineRet`). **Sim store extensions** (strict improvements): (a) `computeTotals` now derives `Check.Amount = sum(ExpenseLineRet.Amount + ItemLineRet.Amount)` and `Deposit.DepositTotal = sum(DepositLineRet.Amount)` when undefined — `bankActivity` walks already preferred line sums but DepositTotal/Check.Amount headers were absent on sim-created entities. Explicit override on create wins (preserves `seedCheck`'s `Amount: 250` pattern). (b) `handleMod`'s post-line-mod block now includes `Check` + `Deposit` — deletes the header field before recompute fires, so a partial line-mod re-derives the new total. (c) `handleQuery`'s `EntityFilter` now matches `PayeeEntityRef` in addition to `CustomerRef`/`VendorRef` — needed for `qb_check_list({payeeName})` to scope (pre-#75 the filter silently returned every check); strict improvement on `qb_bill_payment_list` payee-scope paths that already passed `EntityFilter` against PayeeEntityRef. **Tool-layer conventions** mirror `bills.ts` exactly: same Zod schema patterns for create + mod-style line shapes (txnLineID-keyed merge), same try/catch error wrapper with `qbStatusCodeMessage` humanReadable, same idempotencyKey behavior on create (replay → `idempotentReplay: true`; key+different-payload → 9002), same read-only gate via `assertWritable` (9001). Stale `editSequence` → 3170. **Transfer-specific guard:** self-transfer (same From/To by ListID OR by FullName) rejects at the tool layer with `statusCode 3120` before any wire I/O — real QB enforces this server-side too. Inventory site transfers (Enterprise-only `TransferInventoryAddRq`) intentionally NOT exposed; flagged in tool description + carried to #80. **Tests:** 65 new tests across 3 files — [tests/deposits.test.ts](tests/deposits.test.ts) (22), [tests/checks.test.ts](tests/checks.test.ts) (24), [tests/transfers.test.ts](tests/transfers.test.ts) (19). Coverage: sim handler totals derivation (DepositTotal / Check.Amount on create + re-derive on mod), list shape (line-strip default, includeLineItems, txnId / refNumber / payeeName / date filters, paginate iterator), create happy paths (single + multi line, expense + item, EntityFilter with PayeeEntityRef), validation errors (missing required ref, no-lines rejection on Check, self-transfer rejection), idempotency (replay returns same TxnID with idempotentReplay:true + no duplicate created; conflict surfaces 9002), update flows (header + line mod, line replacement wholesale, total re-derive, stale editSequence 3170, unknown TxnID 500), delete flows (happy path, unknown TxnID 500, read-only gate). [src/index.ts](src/index.ts) instructions block extended with banking-primitives category line + idempotency-keyed tools list updated. [README.md](README.md) tool count 108 → 120 + architecture diagram + new "Banking (Deposit / Check / Transfer)" section right before "Bank Reconciliation". 949 → 1014 tests green. Tool count 108 → 120. **Live verification deferred** — sim-verified for create/list/update/delete + idempotency + read-only gate + self-transfer guard, but `CheckAddRq` / `DepositAddRq` / `TransferAddRq` / their `*ModRq` counterparts haven't been live-pinned against a real QB Desktop. First live exercise should walk: a multi-line Deposit against `Checking`, a Check with mixed expense + item lines, a Transfer between two bank accounts, then a follow-up `qb_uncleared_transactions` + `qb_cleared_status_update` to close the reconciliation loop. If live `*QueryRq` rejects on a missing element class of bug (the schema-order pattern that bit #37), pin the canonical child order in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) the way #37 did for `GeneralSummaryReportQueryRq`.)_
- [ ] **76.** Sales orders — `qb_sales_order_create` / `_list` / `_update` / `_delete` / `_convert_to_invoice`. `SalesOrderAddRq` / `SalesOrderQueryRq` / `SalesOrderModRq`. **Distinct from estimates** (estimates are quotes; sales orders are committed but not yet invoiced — used for backorder / fulfillment workflows). Per [CLAUDE.md](CLAUDE.md) this is a tracked transaction entity but has no tool today.
- [ ] **77.** Sales tax workflows — completely absent. Add: `qb_sales_tax_liability_report` (monthly sales tax owed by agency); `qb_sales_tax_payment_create` (write the actual sales-tax check via `SalesTaxPaymentCheckAddRq`); `qb_sales_tax_code_list` / `qb_sales_tax_item_list` (discover existing codes and items so invoice/SR creation can reference them); `qb_sales_tax_agency_list`. For any client with taxable sales this is a monthly necessity.
- [x] **78.** Time tracking — `qb_time_track_add` / `qb_time_track_list`. `TimeTrackingAddRq` / `TimeTrackingQueryRq`. Foundation for item 70 (`qb_engagement_profitability`) which can't compute hours-per-job without it. Also blocks any service-business billing-by-time workflow. _(Closed 2026-05-14. New tool file [src/tools/time-tracking.ts](src/tools/time-tracking.ts) wired into [src/index.ts](src/index.ts) via `registerTimeTrackingTools`. Pure composite over the existing generic `addEntity` / `queryEntity` / `queryEntityPaginated` / `addEntityIdempotent` primitives — no new manager methods. **TimeTracking is a TRANSACTION** (carries TxnID + EditSequence + TimeCreated/Modified, deletes via TxnDelRq) but **NON-POSTING** (no GL effect, no AR/AP movement, not in `BANK_AFFECTING_TXN_TYPES`). Added to the three transaction-type lists in sync: `buildDeleteRequest`'s isTransaction in [src/qbxml/builder.ts](src/qbxml/builder.ts), `deleteEntity`'s isTransaction in [src/session/manager.ts](src/session/manager.ts), `isTransactionType` in [src/session/simulation-store.ts](src/session/simulation-store.ts) — required for `handleAdd` to use TxnID (not ListID) on persist. `TimeTrackingRet` added to parser `arrayElements` in [src/qbxml/parser.ts](src/qbxml/parser.ts) so single-hit responses still surface as arrays. **API:** `qb_time_track_add` takes `entityName | entityListId` (REQUIRED — the WORKER, Employee/Vendor/OtherName; NOT the customer) + `hours: number | duration: string` (REQUIRED — exactly one; duration wins when both supplied; hours auto-converts via `formatHoursAsDuration` → ISO 8601 PT-H-M-S) plus optional `txnDate` (default today) / `customerName | customerListId` (customer billed against) / `itemServiceName | itemServiceListId` / `className` / `payrollItemWageName` / `notes` / `billable` / `idempotencyKey`. Emits both `IsBillable` (boolean) and `BillableStatus` ('Billable' | 'NotBillable') when `billable` is set; both absent when unset (default state). `qb_time_track_list` takes `txnId` / `entityName | entityListId` (worker — applied SERVER-SIDE via EntityFilter) / `customerName | customerListId` (POST-FILTERED at the tool layer because QB's TimeTrackingQueryRq has NO CustomerFilter — combining with paginate may miss matches beyond the page) / `fromDate` / `toDate` / `billableOnly` (POST-FILTERED) / `maxReturned` / `paginate` / `iteratorID`. Each returned row carries a derived `hours` field (parsed from Duration) for convenience. **Sim handler improvement (strict, paired with #78):** `handleQuery`'s `EntityFilter` fallback chain now starts with `EntityRef` (the worker) BEFORE `CustomerRef` / `VendorRef` / `PayeeEntityRef`. Required because TimeTracking is the ONLY transaction type with BOTH `EntityRef` (worker) AND `CustomerRef` (customer); per QB SDK spec, EntityFilter on TimeTrackingQueryRq targets the worker, so EntityRef must win. No existing transaction type has EntityRef populated, so the reorder is observationally a no-op for every other caller. **Pure helpers `parseDurationToHours` + `formatHoursAsDuration` exported** from the tool module — both run round-trip (within rounding tolerance), reject malformed input (`P1D` day component, `8 hours` free-form text, empty `PT`, negative hours), and emit canonical `"PT0H"` for the zero-hours edge so the round-trip stays valid. **Sim seed (paired):** Five TimeTracking entries seeded in [src/session/simulation-store.ts](src/session/simulation-store.ts) `seedData()` against the existing Alice/Bob/Carla employees + Acme/Global Industries customers + Consulting Services item: 3 billable Alice entries (8h + 6h30m + 4h15m), 1 non-billable Bob admin entry (2h), 1 unlabeled Carla payroll-tracking entry (8h). Mix of dates within the same week + mix of billable states + presence/absence of CustomerRef so list filters all have something to exercise. 46 new tests in [tests/time-tracking.test.ts](tests/time-tracking.test.ts) across 4 layers: (1) pure helpers — 19 covering parse/format edges, malformed input, round-trip; (2) sim handler — 5 covering seed count, add round-trip, EntityFilter by FullName + ListID against EntityRef, date range; (3) `qb_time_track_list` — 10 covering default count, hours derivation, txnId / entityName / entityListId / customerName / customerListId / date range / billableOnly filters, paginate auto-defaults; (4) `qb_time_track_add` — 12 covering hours-to-duration conversion, duration override winning over hours, billable true/false/unset states, missing-entity / missing-duration validation errors, entityListId form, payrollItemWageName + className refs, idempotency replay + 9002 conflict, read-only 9001. 1037 → 1083 tests green. Tool count 121 → 122. **Live verification deferred** — sim-verified end-to-end, but `TimeTrackingAddRq` / `TimeTrackingQueryRq` haven't been live-pinned against a real QB Desktop. The wire is generic (uses `buildAddRequest("TimeTracking", ...)` / `buildQueryRequest("TimeTracking", ...)`) so a schema-order class of bug would surface as statusCode -1 on first live call. If that happens, capture the envelope via `QB_DEBUG_QBXML=1` and pin canonical child order in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) the way #37 did for `GeneralSummaryReportQueryRq`.)_
- [ ] **79.** Vehicle mileage — `qb_vehicle_mileage_add` / `_list` / `qb_vehicle_list`. `VehicleMileageAddRq` / `VehicleMileageQueryRq` / `VehicleQueryRq`. Tax-practice staple (Schedule C / Form 4562 mileage logs).
- [ ] **80.** Inventory adjustments — `qb_inventory_adjustment_add` / `_list`. `InventoryAdjustmentAddRq` / `InventoryAdjustmentQueryRq`. For shrinkage, count corrections, write-offs of damaged stock. Required for any client with inventory items (Phase 1 item 22 split the item store by type but never wired the adjustment surface).
- [ ] **81.** Statement charges — `qb_statement_charge_add` / `_list` / `_update` / `_delete`. `StatementChargeAddRq` / `StatementChargeQueryRq`. Mentioned in item 72 (generic transaction list) as one of the 6 transaction types but has no dedicated tool. Used by service businesses for time-and-materials billing without a formal invoice.

## Phase 18 — Engineering robustness

Operability gaps that bite when running unattended or diagnosing live failures.

- [x] **82.** QB edition / version detection — `qb_host_query` exposes `HostQueryRq` results (`HostQueryRs.ProductName`, `MajorVersion`, `MinorVersion`, `Country`, `SupportedQBXMLVersionList`, `IsAutomaticLogin`, `QBFileMode`). Gates features by edition: item 55 (`qb_w2_summary` = Payroll subscription required), item 66 (`qb_audit_log` = Enterprise required). Surface edition in item 67 (`qb_session_status`) so agents can decide which tools are available without trial-and-error. Run on session connect; cache in `QBSessionManager`, cleared on `qb_company_open`. _(Closed 2026-05-12. Shipped as a thin tool over `queryEntity("Host", {})` — HostQueryRq has no request body, so the generic `buildQueryRequest` emits the correct envelope (`<HostQueryRq requestID="1"></HostQueryRq>`) without a dedicated builder. Sim handler is the existing generic `handleQuery` path against a newly-seeded Host singleton in [src/session/simulation-store.ts](src/session/simulation-store.ts) `seedData()` (`Premier Accountant Edition 2024` / `MajorVersion 34` / 19-entry SupportedQBXMLVersionList / `QBFileMode SingleUser` — most-common CPA-firm edition + exercises the Accountant branch of `deriveHostEdition`). LAZY caching at `QBSessionManager` rather than eager-on-`openSession` — first `getHostInfo()` call hits the wire, subsequent calls return the cached normalized `HostInfo` until `switchCompanyFile` invalidates it. Pass `refresh:true` to force a re-query (rare — for QB Desktop upgraded mid-process). Pure helpers `deriveHostEdition` + `normalizeHostInfo` exported from [src/session/manager.ts](src/session/manager.ts) for testing. **Derived `edition` enum** (`Pro` | `Premier` | `PremierAccountant` | `Enterprise` | `EnterpriseAccountant` | `Unknown`) is the gating signal — tools should never parse `productName` directly. Match order favors Enterprise over Premier (an Enterprise build with "Premier" in marketing copy still classifies as Enterprise). Standalone "Accountant" SKU ("QuickBooks Accountant Desktop") classifies as `PremierAccountant` — it's a rebranded Premier Accountant. Boolean convenience flags `isEnterprise` + `isAccountant` collapse the enum for the common gating pattern. `maxQbxmlVersion` is derived numerically (not lex — `9.0 < 16.0` only works under numeric tuple compare). **Tool surface**: optional `refresh:boolean` arg; response carries the normalized HostInfo + `cached:boolean` (whether the response came from cache, snapshot BEFORE the call) + `simulationMode:boolean`. Standard tool-error wrapper with `humanReadable` from `qb-status-codes.ts`. 30 new tests in [tests/host-query.test.ts](tests/host-query.test.ts) across 4 layers: (1) pure helpers — `deriveHostEdition` across every product-name family + case-insensitivity + standalone-Accountant edge + Unknown fallback; `normalizeHostInfo` multi-version + single-version coercion + numeric max-version + boolean coercion + missing-field defaults + derived-flag composition; (2) sim handler — `queryEntity("Host", {})` returns the seeded shape via generic handleQuery; (3) manager — caching (object-identity check), `refresh:true` bypass, `peekHostInfoCache` accessor, `switchCompanyFile` invalidation; (4) tool surface — registered, returns normalized HostInfo with derived fields, `cached:false`→`true` across calls, `refresh:true` resets cached flag, structured error path. 712 → 742 tests green. Tool count 98 → 99. Design rationale logged in [DECISIONS.md](DECISIONS.md#2026-05-12--qb_host_query-ships-as-a-thin-wrapper-over-queryentityhost--with-manager-level-lazy-caching). **Live verification deferred** — sim seed values are deterministic; live exercise against `VR Tax & Consulting Inc..qbw` will reveal the actual ProductName / MajorVersion / SupportedQBXMLVersionList / QBFileMode QB returns. If the live response shape includes fields beyond the seven seeded (e.g. `Beta`, `IsHostedOnline`), `normalizeHostInfo` drops them silently — extending the type to surface them is one-line work.)_
- [x] **83.** Persistent debug logging — `QB_DEBUG_QBXML=1` writes every QBXML request envelope + raw response to a rotating file (default `./logs/qbxml-YYYYMMDD.log`, configurable via `QB_DEBUG_LOG_PATH`). Needed to diagnose item 37 (P&L parse error) and every future schema-order-class bug — currently the only way to see the wire is to add `console.error` and rebuild. Redact any field flagged sensitive (vendor TIN, bank account number) before writing. _(Closed 2026-05-12. New `QbxmlLogger` module in [src/util/qbxml-logger.ts](src/util/qbxml-logger.ts) — env-gated by `QB_DEBUG_QBXML=1`, process-wide singleton accessed via `getQbxmlLogger()`. Hooks into [src/session/manager.ts](src/session/manager.ts) `sendRequest` — the single chokepoint every QBXML envelope (live + sim) flows through, so one diff covers both modes. Writes paired `request` / `response` entries with per-process monotonic `seq=N` for unambiguous concurrent-pair correlation. Date-stamped filename (`qbxml-YYYYMMDD.log`) is recomputed per-write so long-running processes roll over at midnight without restart; default dir `./logs`, overridable via `QB_DEBUG_LOG_PATH`. Tag-regex redaction (conservative four-field list: `VendorTaxIdent` / `SSN` / `BankAccountNumber` / `CreditCardNumber`) runs before each write. First write failure latches `disabled=true` and stderr-warns once so a logging fault never poisons request flow. Live mode logs raw response XML BEFORE parsing (a parser throw is one of the primary use cases — the wire bytes are the only useful artifact at that point); sim mode JSON-stringifies the parsed `QBXMLResponse`. Synchronous `appendFileSync` so the envelope is on disk before the next line of code runs. Startup banner in [src/index.ts](src/index.ts) prints the resolved log path when enabled. 29 new tests in [tests/qbxml-logger.test.ts](tests/qbxml-logger.test.ts) across 5 layers: (1) singleton resolution — env gating with various string values, default + custom log dir, caching, reset; (2) redaction — each sensitive tag, multiple instances, empty tags left alone, idempotent; (3) file output format — header + body block, seq pairing, durationMs surfacing, JSON stringification of non-string bodies, date-stamped file, op=error shape, auto-mkdir; (4) integration with `QBSessionManager.sendRequest` — no file when unset, paired entries in sim mode, error path via patched `store.processRequest`, multi-call accumulation; (5) defensive — directory-creation failure disables logger without throwing. Tool count unchanged (no new tools — operability infra). 683 → 712 tests green. Directly unblocks the deferred Phase 11 #54 live SCF verification: first live SCF run can now write the actual `TextRow` / `SubtotalRow` / `TotalRow` labels to disk for direct inspection. Design rationale in [DECISIONS.md](DECISIONS.md#2026-05-12--persistent-qbxml-wire-logger-phase-18-83-hooks-at-the-managersendrequest-chokepoint).)_
- [x] **84.** Connection robustness — auto-reconnect on transient `QBXMLRP2` failures (status `0x80040408` "QBSession not open" surfaces when QB Desktop briefly stalls), exponential backoff (250ms → 500ms → 1s → fail) capped at 3 retries. Graceful `EndSession` + `CloseConnection` on `SIGTERM` / `SIGINT` so QB doesn't carry a stuck session into the next launch. Currently a Ctrl+C mid-call leaves QB Desktop with a half-open session that some operators have reported requires a QB restart to clear. _(Closed 2026-05-12. The SIGTERM/SIGINT graceful-shutdown half landed earlier with the live-mode work in [src/index.ts](src/index.ts) `gracefulShutdown`; this session shipped the remaining auto-reconnect-on-transient-failure half. New pure helper `isTransientLiveError` exported from [src/session/manager.ts](src/session/manager.ts) — conservative whitelist matching `0x80040408` hex, decimal `-2147220472`, and the descriptive `"QBSession not open"` text (case-insensitive). Deliberately does NOT match `0x80040409` / RPC_E_* codes / QB-status-code-shaped errors — those have no evidence of being recoverable by reconnect and would waste the retry budget. Frozen exported constant `RECONNECT_BACKOFF_MS = [250, 500, 1000]` pins the schedule; total worst-case sleep budget 1.75s for the 4 wire calls. Retry loop lives in new private `sendLiveRequestWithRetry` method on `QBSessionManager` (called from `sendRequest`'s live branch only — sim mode unaffected by design). Reconnect path runs through new private `reconnectAfterTransientError`: best-effort `EndSession` + `CloseConnection` on the stale ticket (both swallow throws — ticket likely already dead), null out `rp` + `session`, re-run `openSession`. Persistent state survives reconnect: `readOnly` flag, `idempotencyCache`, `hostInfoCache` all intentionally preserved (the underlying invariants are per-process, not per-session). Sleep impl is an instance field `sleepImpl` (overridable via `(sm as any).sleepImpl = stub` for tests — matches existing `store.processRequest` patch pattern). Per-attempt logger calls (`logRequest` / `logResponse` / `logError`) so the QB_DEBUG_QBXML log file shows the full retry sequence with separate seq= markers for each attempt. Per-retry stderr `console.error` for live operability ("Transient QBXMLRP2 error on attempt 1/4 — Reconnecting and retrying after 250ms"). Reconnect-during-reconnect failure path throws a wrapped error naming both the reconnect cause AND the original transient error (the retry budget is forfeit at that point — if QB Desktop won't accept a fresh `OpenConnection2` right now, looping won't help). 21 new tests in [tests/connection-robustness.test.ts](tests/connection-robustness.test.ts) across 3 layers: (1) pure helper `isTransientLiveError` — exact hex/decimal/descriptive matches, case-insensitivity, non-Error inputs, RPC_E_* deliberately excluded; (2) `RECONNECT_BACKOFF_MS` schedule pinned `[250, 500, 1000]` + `Object.isFrozen`; (3) end-to-end retry via fake-live manager (patches `simulationMode=false`, stubs `rp.ProcessRequest` with a behavior queue, stubs `openSession` so reconnect path runs without loading winax, captures `sleepImpl` durations) — initial success no-retry no-sleep, single-transient-then-success with one 250ms sleep, persistent 4-fail throws after 250/500/1000 sleeps with 4 ProcessRequest calls + 3 reconnects, non-transient throws immediately, mixed transient-then-non-transient (second error wins, one sleep), reconnect-failure wraps both causes, console.error visibility, matching backoff durations per attempt, readOnly survives reconnect, descriptive-text-only match without hex code, swallows EndSession/CloseConnection throws during teardown, sim mode does NOT retry. 798 → 819 tests green. No new tools; no schema changes. Design rationale logged in DECISIONS.md.)_
- [x] **85.** Closing date / year-end lock — `qb_closing_date_set({ closingDate, password })` and `qb_closing_date_get`. _(Closed 2026-05-12. **SDK verification result: read path viable, write path does NOT exist at any qbXML version through 16.0.** Verified by grep across the qbwc/qbxml master schema mirrors (qbxmlops20.xml → qbxmlops140.xml — `PreferencesModRq` / `AccountingPreferencesModRq` / `CompanyActivityModRq` all return zero hits). `ClosingDatePasswordIsSet` also does NOT exist at any qbXML version. Shipped in fresh [src/tools/preferences.ts](src/tools/preferences.ts): `qb_closing_date_get` wraps `PreferencesQueryRq` (read path: `PreferencesQueryRs.PreferencesRet.AccountingPreferences.ClosingDate` — DATETYPE field, present since qbXML 2.0), returns ISO closingDate (or null when unset) + four adjacent AccountingPreferences flags (`isUsingAuditTrail`, `isUsingClassTracking`, `isUsingAccountNumbers`, `isRequiringAccounts`) + a human-readable `note` summarizing protection state. The handoff's `DataEventCallbackAddRq` reference was a red herring — that's event-subscription, not preference mutation. `qb_closing_date_set` is an **informational stub** — always fails fast with new synthetic statusCode **9005** + a 9-step QB Desktop UI navigation path (Edit → Preferences → Accounting → Company Preferences → Set Date/Password) that quotes the operator-supplied date and (if supplied) password; no wire I/O; performs the failure synchronously. New `Preferences` singleton seed in [src/session/simulation-store.ts](src/session/simulation-store.ts) `seedData()` carries the full qbXML AccountingPreferences / FinanceChargePreferences / JobsAndEstimatesPreferences / etc. group set; `ClosingDate` defaults to `null` (most-common mid-year case). New synthetic 9005 entry in [src/util/qb-status-codes.ts](src/util/qb-status-codes.ts) ("Closing date cannot be set via the QuickBooks Desktop SDK ... Set the closing date manually in QB Desktop under Edit → Preferences → Accounting → Company Preferences → Set Date/Password."). Pure `normalizeClosingDate` helper exported for testing (accepts ISO date strings, rejects everything else including the fast-xml-parser-emitted empty-object `{}` form for `<ClosingDate/>` self-closing tags). 24 new tests in [tests/closing-date.test.ts](tests/closing-date.test.ts) across 4 layers — pure helper, sim handler via `queryEntity("Preferences", {})`, `qb_closing_date_get` tool surface (default-null + set + malformed-normalize-to-null + boolean coercion for wire-form "true" + error wrapping), `qb_closing_date_set` tool surface (always-fail + 9005 + humanReadable + requestedClosingDate echo + 9-step uiInstructions + password supplied/omitted branches + sdkLimitation/workaround fields + Zod ISO rejection + processRequest spy proving no wire I/O). 819 → 843 tests green (was 819 + 24). Tool count 103 → 105. Wired in [src/index.ts](src/index.ts) via `registerPreferenceTools`. Design rationale + grep evidence + qbXML source citations logged in [DECISIONS.md](DECISIONS.md#2026-05-12--closing-date-is-read-only-via-qbxml-sdk-phase-18-85--qb_closing_date_get-hits-the-wire-qb_closing_date_set-is-an-informational-stub-statuscode-9005). **Live verification deferred** — sim seed assumes the qbxmlops140.xml `PreferencesRet.AccountingPreferences.ClosingDate` path; first live exercise will confirm the actual wire shape against real QB Desktop. If real QB surfaces additional fields (e.g. ClosingDatePasswordIsSet on a future edition), extend tool surface — one-line work.)_
- [x] **86.** MCP prompts (workflow bundles) — register pre-canned workflow guides via the MCP `prompts` API alongside `tools`. _(Closed 2026-05-12. Five workflow-bundle prompts shipped in fresh [src/prompts/workflows.ts](src/prompts/workflows.ts) via `McpServer.registerPrompt`, wired through new `registerWorkflowPrompts(server)` in [src/index.ts](src/index.ts). New `prompts: {}` entry added to server capabilities so the SDK advertises `prompts/list` + `prompts/get` handlers on the wire. **The five prompts:** `/month_end_close` (full month-end checklist — bank rec → CC rec → P&L review → AR/AP aging → BS reconcile → SCF; defaults to prior calendar month; composes 12+ tools), `/credit_card_qb_batch` (bulk-categorize CC statement → atomic `qb_journal_entry_batch_create` with idempotencyKey + 9002 conflict semantics + refund reversal mechanics; bridges the operator's `credit-card-qb-batch` skill), `/trial_balance_workup` (TB + cross-checks: BS reconcile / AR-AP totals / P&L netIncome plug; specifies workpaper output table; bridges the `trial-balance-workup` skill), `/cc_statement_validator` (three-way CC reconciliation: balance match + line-by-line match + discrepancy scan + clear-on-match; bridges the `cc-statement-validator` skill), `/w2_prep` (qb_w2_summary + qb_employee_list + reconciliation against P&L wage totals + balance-sheet withholding liability; calls out 9003/9004 status codes). **Design choices:** every prompt emits a single user-role text message (no assistant pre-fills — keeps prompts portable across host LLMs); every argsSchema is fully optional with sensible defaults computed inline (`priorCalendarMonth()` for month-end, `lastCompletedYear()` for W-2, `todayISO()` for TB/CC validator); prompt bodies hard-code exact `qb_*` tool names so the agent's tool-use loop maps quoted names to actual calls; prompt bodies live in TypeScript (not external `.md` templates — keeps build simple, lets prompts compute dynamic defaults). **Type-safe heterogeneous registration:** the SDK's `registerPrompt<Args>` is generic and rejects an `Array<{argsSchema: Record<string, ZodTypeAny>}>` because union widening forces an incompatibly-loose callback signature; resolved by a `reg<Args>(entry)` helper function that captures each entry's narrow Args type via inference, kept in a const-tuple (`as const`) that preserves the per-entry argsSchema ↔ callback link through the iteration. Pure date helpers (`todayISO`, `lastCompletedYear`, `priorCalendarMonth`) exported for testing — month-boundary handling covers Jan→Dec rollover and leap February. 33 new tests in [tests/mcp-prompts.test.ts](tests/mcp-prompts.test.ts) across 3 layers — pure date helpers, registration plumbing (`registerWorkflowPrompts` calls `server.registerPrompt` exactly 5× with matching name/title/desc/schema), per-prompt callback output (every prompt's message body contains expected tool references + substitutes operator's args + falls back to generic instructions when args are unset) + final shape-validity check across all 5 prompts. 843 → 876 tests green (was 843 + 33). Tool count unchanged at 105 (prompts ≠ tools). Server instructions block in [src/index.ts](src/index.ts) extended with a new "Workflow prompts" section enumerating the five slash-commands. Design rationale (single-message bodies, optional args, type-safe heterogeneous registration) logged in [DECISIONS.md](DECISIONS.md#2026-05-12--mcp-prompts-as-workflow-bundles-phase-18-86--5-prompts-in-srcpromptsworkflowsts-each-emits-one-user-role-text-message-bridges-the-operators-existing-skill-workflows). **Live verification deferred** — first exercise from Claude Desktop will confirm prompts surface in the host UI (typically as slash-commands) and that the host LLM actually follows the recommended tool sequence in the prompt bodies. If Claude Desktop's prompt picker doesn't surface them, check the server's `prompts/list` handler for capability-advertisement errors.)_
