# QuickBooks Desktop MCP — Fix List

Comprehensive list of fixes required to make the server work flawlessly for personal use. Ordered by recommended execution sequence (simulation correctness first so later changes can be tested in dev, live COM last because it requires a Windows + QB Desktop box to verify).

---

## Phase 1 — Simulation correctness (fix dev environment first)

So that everything below can actually be verified without a Windows box.

- [x] **15.** Add `EntityFilter`, `TxnDateRangeFilter`, `PaidStatus`, `RefNumber`, `ModifiedDateRangeFilter` handling to `simulation-store.handleQuery` — currently silently ignored, so transaction list queries return ALL records regardless of filter.
- [x] **17.** Convert input `*LineAdd` arrays to `*LineRet` arrays in simulation responses (with `TxnLineID`, computed `Amount = Quantity * Rate`) so downstream tools see proper line breakdown.
- [x] **16.** Compute `Subtotal`, `SalesTaxTotal`, `BalanceRemaining`, `AppliedAmount`, `IsPaid` in `simulation-store.handleAdd` for invoices/bills/estimates so simulated responses match real QB shape.
- [x] **18.** Update customer/vendor balances in simulation store when invoices/bills/payments are added/applied/deleted, so AR/AP aging reports reflect activity in dev.
- [x] **22.** Multi-store Item simulation: split the single `Item` store into `ItemService` / `ItemInventory` / `ItemNonInventory` / `ItemOtherCharge` / `ItemGroup` stores keyed by `ItemType`, return correct `*Ret` element name (`ItemServiceRet` etc.) so parser's `arrayElements` set actually matches.

## Phase 2 — Item request types (touches schema everywhere)

- [x] **2.** Fix Item request types: replace generic `ItemQueryRq` / `AddRq` / `ModRq` with per-type variants (`ItemServiceQueryRq`, `ItemInventoryQueryRq`, `ItemNonInventoryQueryRq`, `ItemOtherChargeQueryRq`, `ItemGroupQueryRq`) in [tools/items.ts](src/tools/items.ts) and route through manager based on `itemType` arg.
- [x] **3.** Fix Item delete to use correct `ListDelType` subtype (`ItemService`, `ItemInventory`, etc.) — currently sends `'Item'` which QB rejects.

## Phase 3 — Transaction completeness (the workflows that matter)

- [x] **4.** Add `ExpenseLineAdd` and `ItemLineAdd` support to `qb_bill_create` — accept `lines: [{accountName, amount, memo}]` and `lines: [{itemName, quantity, cost}]`, so bills actually post to GL accounts.
- [x] **5.** Add `AppliedToTxnAdd` support to `qb_payment_receive` — accept `appliedTo: [{txnId, amount, discountAmount?}]` so payments actually close out invoices and reduce customer balances.
- [x] **7.** Implement `qb_bill_update` tool (`BillModRq`) — header fields plus `ExpenseLineMod` / `ItemLineMod` support, register in [index.ts](src/index.ts).
- [x] **6.** Add `InvoiceLineMod` support to `qb_invoice_update` — accept `lines` arg with optional `txnLineID` (existing line) or `'-1'` (new line) and build `InvoiceLineMod` blocks.
- [x] **8.** Add `qb_payment_apply` tool — apply an existing unapplied `ReceivePayment` to specific invoices via `ReceivePaymentMod` + `AppliedToTxnMod`.
- [x] **9.** Add `qb_bill_pay` tool — record `BillPaymentCheck` or `BillPaymentCreditCard` against existing bills (currently no way to mark a bill as paid).

## Phase 4 — Missing tools / coverage gaps

- [x] **10.** Add `qb_account_delete` / `qb_account_make_inactive` — currently only list/add/update; needed to deactivate unused accounts (QB usually disallows hard delete if transactions exist, so `make_inactive` is the correct primary).
- [x] **11.** Add `qb_employee_delete` / `make_inactive` (currently only list/add/update).
- [x] **12.** Add missing transaction tools: ~~`qb_sales_receipt_*`~~ (done 2026-04-26), ~~`qb_credit_memo_*`~~ (done 2026-04-26), ~~`qb_purchase_order_*`~~ (done 2026-04-26), ~~`qb_journal_entry_*`~~ (done 2026-04-26).
- [x] **13.** Add `qb_estimate_update`, `qb_estimate_delete`, `qb_estimate_convert_to_invoice` tools — currently only list/create.
- [x] **30.** Add `Class`, `Terms`, `PaymentMethod`, `SalesRep`, `CustomerType`, `VendorType` list tools — needed because invoice/bill creation references these by `FullName` but there's no way to list/discover them.

## Phase 5 — Reporting (currently mostly fake)

- [x] **14.** Implement real `CompanyQueryRq` in `qb_company_info` — return company name, legal name, address, fiscal year start, tax form, etc., not just session state.
- [x] **19.** Implement `asOfDate` filtering in `qb_ar_aging` and `qb_ap_aging` — currently the param is decorative; should filter open transactions by date and bucket by 0-30 / 31-60 / 61-90 / 90+ days.
- [x] **20.** Add proper P&L and Balance Sheet report tools (`GeneralSummaryReportQueryRq` with `ReportType=ProfitAndLossStandard` / `BalanceSheetStandard`) — current "reports" are just account-balance rollups.
- [x] **21.** Add date-range support to `qb_balance_summary`, and group by `AccountType` in canonical QB order (Bank, AccountsReceivable, OtherCurrentAsset, ... Equity, Income, COGS, Expense).

## Phase 6 — Plumbing, validation, ergonomics

- [x] **23.** Fix `QB_SIMULATION` env semantics in [session/manager.ts:51-53](src/session/manager.ts#L51-L53) — currently `QB_SIMULATION=false` on Windows still simulates unless `QB_LIVE=1` is also set; either honor `QB_SIMULATION=false` alone or document the actual rule and align README.
- [x] **24.** Remove dead code: `parseQBXMLResponse` import in [session/manager.ts:27](src/session/manager.ts#L27), `buildSingleRequest` export in [qbxml/builder.ts:66](src/qbxml/builder.ts#L66), `QBXMLRequestBody` import in [qbxml/builder.ts:9](src/qbxml/builder.ts#L9), and the useless ternary `isTransaction ? id : id` at [simulation-store.ts:214](src/session/simulation-store.ts#L214).
- [x] **25.** Wrap `session.queryEntity` / `addEntity` / `modifyEntity` / `deleteEntity` calls in tool handlers with try/catch — translate `QBXMLResponseError` into structured tool error responses (`isError: true` with `statusCode` + `statusMessage`) instead of letting them propagate as raw exceptions.
- [x] **26.** Add status code mapping table for common QB errors (3120 missing field, 3170 modify failed, 3260 insufficient permission, 500 not found, etc.) so tool errors are user-readable.
- [x] **27.** Add `IteratorID` / `IteratorRemainingCount` support to large queries (Customer, Invoice, Bill, Item) — real QB caps at ~500 rows and returns an iterator handle for pagination.
- [x] **28.** Validate `AccountType` enum in `qb_account_add` against QB's allowed values (`Bank`, `AccountsReceivable`, `OtherCurrentAsset`, `FixedAsset`, `OtherAsset`, `AccountsPayable`, `CreditCard`, `OtherCurrentLiability`, `LongTermLiability`, `Equity`, `Income`, `CostOfGoodsSold`, `Expense`, `OtherIncome`, `OtherExpense`, `NonPosting`).
- [x] **29.** Add minimal input validation: email format, phone, postal code, ISO date strings (`YYYY-MM-DD`) on date fields — currently any string passes through to QB which then rejects with cryptic errors.

## Phase 7 — Live mode (last, requires Windows + QB Desktop to verify)

- [x] **1.** Implement live `QBXMLRP2` COM connection in [session/manager.ts](src/session/manager.ts) (add `winax` / `node-activex` dep, replace throws in `openSession` / `sendRequest` with real `OpenConnection2` / `BeginSession` / `ProcessRequest` / `EndSession` / `CloseConnection` calls, wire `parseQBXMLResponse` for live responses). _(Closed 2026-05-09. winax 3.4.2 prebuilt for Node 20.20.2 extracted via `npm install`. `node scripts\verify-live-connection.mjs` returned full company data for `VR Tax & Consulting Inc.` (EIN 84-2215991, Form 1120S). Live MCP transport verified end-to-end via [scripts/exercise-mcp-live.mjs](scripts/exercise-mcp-live.mjs): 10/10 read-only tools (qb_company_info, qb_account_list, qb_customer_list, qb_vendor_list, qb_item_list, qb_invoice_list, qb_bill_list, qb_employee_list, qb_class_list, qb_terms_list) returned structured live data after a schema-ordering fix — see notes in HANDOFF.md.)_
- [x] **34.** Add `qb_company_open` tool — accept `companyFile` arg (absolute or UNC path), close any active session (`EndSession` + `CloseConnection`), update `QBSessionManager.config.companyFile`, open a new session against the new file. In simulation mode it just swaps the path string and resets `SimulationStore` to fresh seed (so the LLM observes a "different company" with its own books). Required so the operator can move between client `.qbw` files in one chat session without restarting the server. Behavior must be observationally identical between live and simulation per NF1. Register in [src/index.ts](src/index.ts), add to `instructions` block, document in README session-tools table. _(Closed 2026-05-09. Added `QBSessionManager.switchCompanyFile(path)` + `getCompanyFile()` in [src/session/manager.ts](src/session/manager.ts); tool registered alongside qb_company_info in [src/tools/reports.ts](src/tools/reports.ts). Sim-mode response carries `simulationStoreReset: true` so the LLM observes the discontinuity. Sim-fidelity tradeoff documented in [DECISIONS.md](DECISIONS.md#2026-05-09--company-switching-reseeds-the-simulation-store).)_
- [x] **35.** Add `qb_company_list` tool — read a configured root folder (new env `QB_COMPANY_ROOT`, default to the directory of `QB_COMPANY_FILE`), enumerate `*.qbw` files, return `[{ companyFile, displayName, sizeBytes, modifiedAt }]`. Pure filesystem operation; works identically in live and simulation. Lets the LLM discover the operator's client list by name without the operator typing UNC paths. Register in [src/index.ts](src/index.ts), add to `instructions` block, document in README. _(Closed 2026-05-09. Resolves search root in priority order: `root` arg → `$QB_COMPANY_ROOT` → `dirname($QB_COMPANY_FILE)`. Returns sorted-by-modifiedAt-desc results so the most-recent client surfaces first. `.env.example` documents the new var.)_
- [x] **36.** Add a fresh `tests/company-switching.test.ts` (Vitest) covering: (a) `qb_company_open` swaps file path and seeds a fresh store, (b) data created against company A is not visible after switching to company B, (c) switching back to A returns A's seed (NOT the prior session's mutations — confirms reset is intentional and documented), (d) `qb_company_list` returns expected entries from a temp dir of fixture `.qbw` files. Document the "switching resets sim state" decision in [DECISIONS.md](DECISIONS.md) — it's a deliberate sim-fidelity tradeoff (real QB persists; sim doesn't). _(Closed 2026-05-09. 11 new tests in [tests/company-switching.test.ts](tests/company-switching.test.ts) covering all four scenarios + schema-level empty-string rejection, env-var fallback, root-arg override, missing-root error, missing-directory error, empty-directory case, and end-to-end list→open round-trip. Total suite now 189 passing across 6 files.)_

## Phase 8 — Project hygiene

- [x] **31.** Add `tests/` directory with Vitest: round-trip tests for builder→parser, simulation-store CRUD per entity, filter handling, and tool integration tests through the MCP server transport. _(Closed 2026-04-27. 5 test files in `tests/` cover 178 assertions: `iterator.test.ts` (27), `input-validation.test.ts` (44), `error-shape.test.ts` (47) port the matching `.mjs` harnesses; new `qbxml-roundtrip.test.ts` (40) and `simulation-store.test.ts` (20) cover the builder→parser + CRUD/filter gaps. Run via `npm test`. `verify-item23-env-matrix.mjs` kept as standalone script per [DECISIONS.md](DECISIONS.md#2026-04-27-vitest-for-the-test-harness-keep-env-matrix-as-a-standalone-mjs-script). The 5 `.mjs` harnesses are kept and complementary — they verify the built `dist/`, while Vitest verifies `src/`.)_
- [x] **32.** Add `.gitignore` (`node_modules/`, `dist/`, `*.log`, `.env`), `.env.example` documenting all `QB_*` vars, and run `npm run build` to verify `dist/` produces working node entry. _(Closed 2026-04-27. `.gitignore` already covered required entries plus `.env.local` + `.DS_Store`; `.env.example` shipped under Item 23. Smoke test: `node dist/index.js </dev/null` prints 5-line startup banner with `Mode: simulation` and exits 0.)_
- [x] **33.** Update README tool count and tables once new tools (~~bill_update~~, ~~payment_apply~~, ~~bill_pay~~, ~~account_delete~~, ~~sales_receipt~~/~~credit_memo~~/~~PO~~/~~JE~~, ~~estimate_update/delete/convert~~, ~~supporting list tools~~) are added. _(Closed 2026-04-26 when JE README work landed.)_

---

## Phase 9 — Critical bug fixes (operator-blocking, P0)

Reported by the operator on 2026-05-09 during hands-on Claude Desktop testing against the live `VR Tax & Consulting Inc..qbw` file. These block core CPA workflows.

- [x] **37.** Fix `qb_pnl_report` — currently returns `statusCode -1` "QuickBooks found an error when parsing the provided XML text stream" regardless of params (with/without dates, with/without basis). Single most-used report in any tax + bookkeeping practice; until this works the tool can't answer "what did we make last month" without walking invoices + sales-receipts + credit-memos + JEs (and even then misses JE-only postings). Diagnose: enable QBXMLRP2 request logging, capture the failing payload, compare child order against QB's own `<xs:sequence>` for `GeneralSummaryReportQueryRq` (the 2026-05-09 schema-order class of bug). Pin the fix in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) once root cause is known. _(Closed 2026-05-09. Two halves: (a) schema-order — `buildReportRequest` emitted `ReportBasis` at child position 3, QB requires position 15 after `SummarizeColumnsBy`/`IncludeSubcolumns`; pinned in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts). (b) row-tree adapter — even after the parse error cleared, live returned `<ReportData>` rows instead of `{Sections, Totals}` so payloads were empty; new `adaptLiveReportRet` in [src/qbxml/parser.ts](src/qbxml/parser.ts) translates. Verified live against `VR Tax & Consulting Inc..qbw` FY2024: TotalIncome 516216.51, TotalExpenses 501880.01, GrossProfit 507216.51, NetIncome 14336.5; BS reconciles 341828.33 = 197863.26 + 143965.07. `qb_balance_sheet_report` got the adapter for free. 199/199 tests pass; [scripts/exercise-mcp-live.mjs](scripts/exercise-mcp-live.mjs) extended with 5 report probes; design rationale in [DECISIONS.md](DECISIONS.md#2026-05-09--live-report-shape-adapted-at-the-parser-layer-not-at-each-tool).)_
- [x] **38.** Fix `qb_balance_summary` `asOfDate` filter — currently silently ignored. Returns current snapshot even when called with `asOfDate` and `simulationMode: false`, so the operator gets wrong data without warning. The `asOfNote` field admits this in plain English ("Balance reflects current snapshot, not the requested date range. Historical reconstruction requires walking transactions per account, pending Phase 5 P&L work."). Either implement historical reconstruction or remove the date params and the misleading note. Recommended: implement, since balance-as-of is also table-stakes. _(Closed 2026-05-09. Tool params replaced: `fromDate`/`toDate` → `asOfDate` + `basis`. AS/LI/EQ figures sourced from `runReport("BalanceSheetStandard", { toDate: asOfDate, basis })`; INC/EXP figures from `runReport("ProfitAndLossStandard", { toDate: asOfDate, basis })` (lifetime through asOfDate). Per-account totals bucketed back into the 16-way canonical AccountType order via name→type lookup from a single AccountQuery. NonPosting accounts fall back to `Account.Balance` (not in either report — they don't post to GL). Bucket logic extracted as exported `buildBalanceSummary` for direct unit testing. 15 new tests in [tests/balance-summary.test.ts](tests/balance-summary.test.ts) (8 unit on the helper, 7 end-to-end via session.runReport). Misleading `asOfNote`/`asOfDateRange` dropped. Live exerciser extended with 3 new probes (28/28). Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-09--qb_balance_summary-sources-asliep-from-balancesheetstandard-and-incexp-from-profitandlossstandard). Tradeoff: sim's `BalanceSheetStandard` reads `Account.Balance` for AS/LI/EQ (snapshot — `asOfDate` advisory there); P&L walk IS date-bounded in both modes. Build clean, 214/214 tests green, tool count unchanged at 74.)_
- [x] **39.** Pagination DX — calling any list tool with `paginate: true` and no `maxReturned` returns "There is a missing element: MaxReturned." The tool already knows it needs `MaxReturned` the moment `paginate` flips true. Default `maxReturned` to 500 (QB's effective per-batch cap) when `paginate` is enabled, document the cap in tool descriptions, surface the iterator semantics in error messages. _(Closed 2026-05-09. Coalesce added at the tool handler in all four tools that expose `paginate` — `qb_customer_list`, `qb_invoice_list`, `qb_bill_list`, `qb_item_list`. `iteratorID` alone (mid-pagination Continue) also triggers the default. Explicit `maxReturned` still wins. Tool descriptions updated on all four. 14 new tests in [tests/iterator.test.ts](tests/iterator.test.ts) Layer 8 pin the contract: spy on `session.queryEntityPaginated` to assert `filters.MaxReturned === 500` for the default path, `=== 50` for explicit override, untouched for the non-paginated `queryEntity` path. Error-message rewrite for callers who explicitly trigger "missing element: MaxReturned" deferred to Item 65 per HANDOFF scope. Build clean, 228/228 tests green (was 214 + 14 new). Tool count unchanged at 74.)_

## Phase 10 — High-priority features (operator's top-10 list)

Operator-prioritized list of "if I were maintainer and you were customer" — top features beyond the P0 bugs.

- [x] **40.** `qb_transaction_list_by_account` — any GL account, any date range, with running balance. Without this, cannot reconstruct income, audit a suspicious balance, or pull anything QB calls a "Transaction Report." Wire as `TransactionQueryRq` with `AccountFilter` + `TxnDateRangeFilter` + sort by date; compute running balance server-side from the response. _(Closed 2026-05-09. New tool registered in fresh [src/tools/transactions.ts](src/tools/transactions.ts), wired in [src/index.ts](src/index.ts) (tool 75). Manager exposes `queryTransactions(filters)` that wraps `TransactionQueryRq` via the generic `queryEntity` path — keeps the "tools never construct QBXML directly" rule intact. Sim adds `handleTransactionQuery` cross-type fanout in [src/session/simulation-store.ts](src/session/simulation-store.ts): walks Bill/Check expense+item lines, Invoice/SalesReceipt/CreditMemo income lines via `resolveLineAccount`, and JE debit/credit lines with account-natural-direction sign convention. Sim emits LINE-LEVEL postings only — implicit AR/AP/Bank counter-postings are documented as not surfaced (live QB returns the full posting tree). Running balance computed in the tool handler per HANDOFF math: `openingBalance = Account.Balance − Σ period postings`, then walks forward; exact when toDate ≥ now, approximate (overstated by post-period postings) for historical windows — documented limitation. `includeRunningBalance:false` opts out of the AccountQueryRq round trip. 16 new tests in [tests/transaction-list.test.ts](tests/transaction-list.test.ts) cover sim line-level fanout, JE sign convention (natural-debit Rent Expense vs natural-credit Sales Revenue), date filter, MaxReturned cap, ListID-form AccountFilter resolution, missing AccountFilter (3120 reject), tool error shape, and running-balance math against the seeded snapshot — Rent Expense closes back to its 24000 seed balance after the 3 period postings (2000+2100+500), Sales Revenue closes to 185000 after +700/-150. Plus the schema-order regression pin in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) for `TransactionQueryRq` (MaxReturned → TxnDateRangeFilter → AccountFilter). 244/244 tests green (was 228 + 16 new). Build clean.)_
- [x] **41.** Line-level detail in list responses — invoice / sales-receipt / bill / credit-memo / PO / estimate `*_list` responses currently return header totals only. Add `includeLineItems: boolean` arg (default `false` to preserve current shape). To answer "what services drove April revenue" you'd otherwise need 27 round trips per month. Wire via `IncludeLineItems` in the existing `*QueryRq`. _(Closed 2026-05-09. Scope expanded to seven list tools — added `qb_journal_entry_list` because its description already promised lines on every row and the strip-by-default change would have made it lie. Sim handleQuery now strips `*LineRet` / `*LinesRet` keys via `/Line(s?)Ret$/` regex by default; preserves them when IncludeLineItems is truthy (boolean `true` for in-process callers, string `"true"` for the wire form). Header-derived totals (Subtotal, AmountDue, BalanceRemaining, IsPaid, TotalDebit/Credit, AppliedAmount) survive the strip. AppliedToTxnRet (relationship array) survives — its key doesn't match the regex. `qb_estimate_convert_to_invoice` updated to internally pass `IncludeLineItems: true` so the convert flow doesn't lose its line set. Schema-order positions pinned in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts) for InvoiceQueryRq + BillQueryRq + JournalEntryQueryRq + (looped) Estimate/SalesReceipt/CreditMemo/PurchaseOrder QueryRqs. 42 new tests in [tests/include-line-items.test.ts](tests/include-line-items.test.ts) + builder-emit-order.test.ts. 286/286 tests green, build clean, tool count unchanged at 75.)_
- [x] **42.** Read-only session flag — `qb_session_connect({ readOnly: true })` lets an agent run diagnostic work with zero mutation risk. QBXMLRP2 supports per-call enforcement; gate every `*_add` / `*_update` / `*_delete` / `*_apply` tool to throw a structured error when session is read-only. Big win for confidence in unattended automation. _(Closed 2026-05-10. Single chokepoint inside `QBSessionManager` ([src/session/manager.ts](src/session/manager.ts)): private `readOnly` flag + `setReadOnly` / `isReadOnly` accessors + private `assertWritable(operation)` helper called at the entry of `addEntity` / `modifyEntity` / `deleteEntity` / `executeBatchAdd`. Throws new `QBReadOnlyError` (synthetic statusCode 9001) BEFORE any envelope is built — surfaces through the existing tool-side error wrapper as `isError: true` with `humanReadable` via [src/util/qb-status-codes.ts](src/util/qb-status-codes.ts). Zero per-tool churn — all 47 mutation call sites across 14 tool files automatically inherit the gate. `qb_session_connect` ([src/tools/reports.ts](src/tools/reports.ts)) accepts `readOnly: z.boolean().optional()`; defaults to false on every call (fresh `qb_session_connect()` always re-enables writes). `qb_company_info` surfaces current `readOnly` state. Flag persists across `openSession`/`closeSession`. `switchCompanyFile` deliberately ungated. 25 new tests in [tests/read-only-session.test.ts](tests/read-only-session.test.ts) cover the manager state machine, gate firing on each of the 4 mutation methods, reads staying open, tool-surface propagation through `qb_session_connect` / `qb_company_info`, and structured-error surfacing through `qb_customer_add` / `_update` / `_delete` + `qb_journal_entry_batch_create`. Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-10--read-only-session-flag-gates-at-the-session-manager-not-per-tool). 327/327 tests green. Tool count unchanged at 76 (no new tool — flag-only change).)_
- [x] **43.** `qb_journal_entry_batch_create({ entries: [...] })` — atomic (all-or-nothing). Replaces the Excel intermediate the operator's `credit-card-qb-batch` skill currently produces. Wire as a single QBXML envelope with multiple `JournalEntryAddRq` blocks under `<QBXMLMsgsRq onError="stopOnError">`. Same pattern reused by item 58 (batch invoice/SR). _(Closed 2026-05-10. New tool registered in [src/tools/journal-entries.ts](src/tools/journal-entries.ts); 1–100 entries per call. Upfront balance validation rejects unbalanced batches before any wire I/O. Compensating delete in REVERSE post order on mid-batch failure — tracks `rolled-back` (delete succeeded) vs `orphaned` (delete itself failed; surfaces TxnID for manual cleanup). Generalized the plumbing for #58: [src/session/simulation-store.ts](src/session/simulation-store.ts) `processRequest` now walks array-valued request keys and honors `onError="stopOnError"`; [src/qbxml/parser.ts](src/qbxml/parser.ts) captures `@_requestID` per response; new `session.executeBatchAdd(entityType, entries)` in [src/session/manager.ts](src/session/manager.ts) returns positionally-aligned posted/failed/skipped slots. 16 new tests in [tests/journal-entry-batch.test.ts](tests/journal-entry-batch.test.ts) cover envelope plumbing, parser requestID capture, sim multi-request + stopOnError, full-success path, single-entry batch, upfront-validation rejection, floating-point tolerance, schema bounds (1–100), full rollback path, and orphaned path via mocked delete failure. New multi-request envelope structure pin in [tests/builder-emit-order.test.ts](tests/builder-emit-order.test.ts). Tool count 75 → 76. Design rationale logged in [DECISIONS.md](DECISIONS.md#2026-05-10--qb_journal_entry_batch_create-defaults-to-atomic-with-auto-rollback-compensating-delete). 302/302 tests green.)_
- [x] **44.** `qb_1099_summary` and `qb_1099_detail` — for a 2,000-client practice issuing 1099s every January, conspicuous absence. QBXML: `Form1099QueryRq` (1099-MISC + 1099-NEC). Returns vendor + threshold + tracked amounts. _(Closed 2026-05-10. Two tools registered in fresh [src/tools/form-1099.ts](src/tools/form-1099.ts), wired in [src/index.ts](src/index.ts) (tools 77 + 78). Implementation diverges from the literal `Form1099QueryRq` plan: aggregates server-side from existing typed `Bill` + `Check` queries via `session.queryEntity` rather than wiring a new wire request — keeps the "tools never construct QBXML directly" rule, identical sim/live behavior, no schema-order risk to verify on a Windows box. Vendor classification driven by `IsVendorEligibleFor1099` + `Vendor1099Type` ('NEC' default, 'MISC' opt-in). Threshold defaults to $600 (IRS general TY2024+). Defaults `taxYear` to last completed year. Card payments excluded per IRS rule. Limitation: doesn't honor QB Preferences' per-account 1099 box mapping — every payment to an eligible vendor counts toward the threshold (more permissive than QB's Form1099 wizard, documented in the tool description). Three new 1099-eligible vendors added to the seed (Joe Contractor / Sarah Designer LLC / ACME Property Mgmt) with TIN + address fields; existing vendors got an explicit `IsVendorEligibleFor1099: false`. 24 new tests in [tests/form-1099.test.ts](tests/form-1099.test.ts) cover pure helpers (date-window resolution, vendor classification, line totaling), the aggregation engine (Bill + Check sum, ineligible-vendor exclusion, out-of-window exclusion, empty result), the summary tool (sort order, threshold filter, includeBelowThreshold, formType filter, explicit fromDate/toDate override, address/taxId surfacing, default taxYear), and the detail tool (per-transaction breakdown, vendorListId scope, vendorFullName scope, no-match success, formType filter). Tests use `session.addEntity` to create Bill/Check fixtures rather than seeding them globally — the seed-only-vendors approach kept the existing 327 tests green. 351/351 tests green. Tool count 76 → 78. Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-10--qb_1099_summary--qb_1099_detail-aggregate-from-typed-bill--check-queries-not-form1099queryrq).)_
- [x] **45.** ~~Memorized / recurring transaction CRUD~~ — **CLOSED 2026-05-10 as SDK-blocked.** Verified against qbxmlops130/140 schemas (the qbwc/qbxml master mirrors of Intuit's official SDK XSDs): `MemorizedTransactionListQueryRq` / `MemorizedTxnListQueryRq` / Add / Mod / Del / Execute — **none exist in the QBXML SDK at any version.** The only mention of `memorizedTxn` in the entire 2.77 MB schema is as an enumeration value for `rowType` inside `ListDeletedQueryRq` (i.e. you can query deletion records of memorized templates, not the live list). Intuit's long-standing posture: memorized transactions are managed exclusively through QB Desktop's UI (Ctrl+M on an open transaction) and auto-executed by QB's internal scheduler; the SDK doesn't expose the recurring/memorized list to integrators. Workflow stand-in to address the operator's underlying pain (monthly retainer billing without QB's memorized system): build `qb_invoice_duplicate(sourceTxnId, overrides)` as a Phase 12 follow-up — reads a prior invoice's lines, submits an `InvoiceAddRq` with new TxnDate / RefNumber. Same outcome as right-click "Use" on a memorized template, no SDK gap. Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-10--memorized-transactions-not-exposed-by-qbxml-sdk-45-closed-sdk-blocked).
- [x] **46.** Bank reconciliation primitives — **CLOSED 2026-05-10. Scope narrowed after SDK research.** Original HANDOFF premise was wrong in both directions: `ReconcileQueryRq` / `ReconcileDetail` report type / `LastReconciledDate` field — none exist in the QBXML schema. What DOES exist (and the HANDOFF missed) is `ClearedStatusModRq` — the actual atomic write primitive bank reconciliation is built on (flips one txn or one split line between `Cleared` / `NotCleared` / `Pending`). Shipped `qb_cleared_status_update` ([src/tools/reconciliation.ts](src/tools/reconciliation.ts)) wrapping it. Targets the seven bank-affecting transaction types (Check, BillPaymentCheck, BillPaymentCreditCard, Deposit, Transfer, CreditCardCharge, CreditCardCredit); calls against Invoice/Bill/JE/etc. return statusCode 3120 distinguishing "wrong txn type" (sim probes other-txn-type stores) from 500 "unknown TxnID". Read-only gate composes (9001). Naturally idempotent — no idempotency key arg (would add no value; flipping Cleared on an already-Cleared txn is a server no-op). Read-side (which txns are uncleared) requires `CustomDetailReportQueryRq` with `IncludeColumn=ClearedStatus` — the only QBXML path that returns cleared-status — and pairs structurally with Phase 11 #56 (which has the same custom-report plumbing). Read-side carry: new item **#56a** below. Until that lands, the operator pairs QB Desktop's reconciliation UI (visual: which items need clearing) with `qb_cleared_status_update` bulk calls through an agent (replaces clicking every line by hand). 23 new tests in [tests/reconciliation.test.ts](tests/reconciliation.test.ts) cover the builder schema-order pin, sim handler (NotCleared default on bank-affecting add, header- and line-level flips on Check/Deposit/Transfer/BillPaymentCheck, 500 vs 3120 distinction, enum validation, missing TxnID, missing line ID), manager method (assertWritable gate fires, 9001 sentinel, idempotency cache untouched), and tool surface (happy paths, error surfacing with humanReadable, read-only gate compose). 395/395 tests green. Tool count 78 → 79. Decision logged in [DECISIONS.md](DECISIONS.md#2026-05-10--bank-reconciliation-sdk-surface--clearedstatusmodrq-is-the-actual-primitive-46-closed-with-narrowed-scope).
- [x] **47.** Idempotency keys on creates — optional `idempotencyKey: string` arg on every `*_create` / `*_add` tool. Server stores recent `(key → created TxnID/ListID)` pairs in the simulation store and the live session manager (in-memory, scoped to current `companyFile`); retrying with the same key returns the original result instead of duplicating. Prevents duplicate invoices on agent retry / network glitch. _(Closed 2026-05-10. Single chokepoint inside `QBSessionManager` mirroring the #42 read-only gate template — new `addEntityIdempotent` / `executeBatchAddIdempotent` methods + `Map<key, {entityType, payloadFingerprint, result, createdAt}>` cache, FIFO-bounded at 1000 entries, cleared on `switchCompanyFile`. Fingerprint = SHA-256 of canonicalized JSON (recursive sorted-key normalization, array order preserved). New `QBIdempotencyKeyConflictError` (synthetic statusCode 9002) thrown when same key is reused with a different payload — surfaces through the existing tool error wrapper with humanReadable from [src/util/qb-status-codes.ts](src/util/qb-status-codes.ts). Tool surface: 16 `*_create` / `*_add` handlers across 14 files gain optional `idempotencyKey: z.string().min(1).optional()`; on replay the response carries `idempotentReplay: true`. Failed creates do NOT poison the cache — next retry can fix the underlying problem. Batch tool ([src/tools/journal-entries.ts](src/tools/journal-entries.ts) `qb_journal_entry_batch_create`) only caches full-success outcomes (partial failures would replay through the rollback path and re-attempt deletes of already-removed TxnIDs — documented loudly). `qb_estimate_convert_to_invoice` skips the IsAccepted flip on replay (the original call already ran it; re-running with stale editSequence would fail). Server `instructions` block in [src/index.ts](src/index.ts) extended with an idempotency section listing every keyed tool. 21 new tests in [tests/idempotency-keys.test.ts](tests/idempotency-keys.test.ts) cover the manager state machine (miss / replay / conflict / cross-entityType / different-keys / array-order sensitivity), cache lifecycle (failure-non-poisoning, cross-openSession persistence, switchCompanyFile clears, FIFO eviction at 1000), batch idempotency (full-success replay, partial-failure non-caching, reorder conflict), and tool surface (qb_customer_add propagation + idempotentReplay flag, conflict surfacing with statusCode 9002, no-key fall-through to existing behavior, qb_journal_entry_batch_create end-to-end + upfront-validation non-caching). 372/372 tests green (was 351 + 21). Tool count unchanged at 78 (no new tools — flag-only change). Design rationale logged in [DECISIONS.md](DECISIONS.md#2026-05-10--idempotency-cache-lives-at-qbsessionmanager-fingerprint-matches-on-replay-only-caches-successful-creates).)_

## Phase 11 — Remaining missing reports

CPA-weekly reports beyond Phase 10 priorities.

- [ ] **48.** `qb_customer_balance_detail` — every open + closed transaction per customer, in date order with running balance. Aging summary tells you who owes, not why. QBXML: `CustomerBalanceDetailReportQueryRq`.
- [ ] **49.** `qb_sales_by_customer_summary` and `qb_sales_by_customer_detail` — pricing analysis, capacity planning, lost-client identification. QBXML: `GeneralSummaryReportQueryRq` / `GeneralDetailReportQueryRq` with `ReportType=SalesByCustomerSummary` / `Detail`.
- [ ] **50.** `qb_sales_by_item_summary` and `qb_sales_by_item_detail` — same as above, by item.
- [ ] **51.** `qb_vendor_balance_detail` — analog to customer balance detail. QBXML: `VendorBalanceDetailReportQueryRq`.
- [ ] **52.** `qb_expense_by_vendor_summary` and `qb_expense_by_vendor_detail` — `GeneralSummaryReportQueryRq` / `Detail` with `ReportType=ExpensesByVendorSummary` / `Detail`.
- [ ] **53.** `qb_general_ledger` — period-bounded, account-filterable. Foundation report. QBXML: `GeneralDetailReportQueryRq` with `ReportType=GeneralLedger`.
- [ ] **54.** `qb_statement_of_cash_flows` — `GeneralSummaryReportQueryRq` `ReportType=StatementOfCashFlows`.
- [ ] **55.** `qb_w2_summary` — payroll. **Requires QB Payroll subscription**; QBXML payroll endpoints (`PayrollSummaryReportQueryRq`) only return data when subscribed. Document the dependency in the tool description; return a clear error message if no payroll subscription is detected (rather than the raw QBXML status code).
- [ ] **56.** `qb_reconciliation_discrepancy` and `qb_uncleared_transactions_by_account` — by bank/CC account. Cannot do month-end without it. **Note (added 2026-05-10):** The HANDOFF's claim that `GeneralDetailReportQueryRq ReportType=ReconcileDiscrepancy` / `UnclearedTransactionsByAccount` exists is **wrong** — neither is in the QBXML schema (verified against qbxmlops130/140). The real path is `CustomDetailReportQueryRq` with `IncludeColumn=ClearedStatus` + `AccountFilter` — that's the only QBXML reporting surface that returns cleared-status data. Build the CustomDetailReport infrastructure (builder + row-tree adapter at the parser layer + sim emit) as the first half; scope `qb_uncleared_transactions` and `qb_reconciliation_discrepancy` on top of it. Pairs with the new #56a below and with #46 (which shipped the write primitive).
- [ ] **56a.** `qb_uncleared_transactions` — read-side companion to the #46 `qb_cleared_status_update` write primitive. Build on the CustomDetailReportQueryRq infrastructure landing with #56. Takes `accountName` / `accountListId` + optional `asOfDate`; returns list of bank/CC transactions with `ClearedStatus !== 'Cleared'` (NotCleared + Pending). Required so the operator can discover what needs clearing through the MCP instead of through QB Desktop's reconciliation UI. Sim implementation: walks the seven bank-affecting transaction stores filtered by the txn's bank/CC posting account, filters by `ClearedStatus`. Live: parses the CustomTxnDetail row-tree (similar shape to PnL/BS — adaptLiveReportRet pattern).

## Phase 12 — Remaining missing workflows

- [ ] **57.** `qb_invoice_write_off(txnId, writeOffAccount, memo)` — single call. Currently requires create credit memo + `qb_credit_memo_apply` (two calls plus the `editSequence` dance). Internally executes the same two steps in one tool, atomic.
- [x] **57a.** `qb_invoice_duplicate(sourceTxnId, overrides)` — workflow stand-in for the operator's monthly retainer billing pain that #45 was originally meant to solve. Reads the source invoice's lines via `qb_invoice_list { txnId, includeLineItems: true }`, submits a fresh `InvoiceAddRq` with the carried `CustomerRef` + `lines` and a new `TxnDate` / `RefNumber` (operator-supplied overrides). _(Closed 2026-05-10. Registered in [src/tools/invoices.ts](src/tools/invoices.ts) as a composite tool — no new wire types, no new manager methods. Reads source via `session.queryEntity("Invoice", { TxnID, IncludeLineItems: true })`, builds fresh InvoiceAdd from CustomerRef + InvoiceLineRet → InvoiceLineAdd mapping (drops TxnLineID — new invoice generates its own), carries optional ClassRef/TermsRef/SalesRepRef/PORefNumber header refs, applies operator overrides for txnDate/dueDate/refNumber/memo/customerName/customerListId. Default carry policy: TxnDate → today (NOT carried — duplicating to the same date is rarely what you want), DueDate → unset (it's relative to TxnDate; carrying makes no sense for a different date), RefNumber → unset (avoids collisions on monthly retainer flows; operator supplies or QB autonumbers), Memo → "Duplicate of <source ref or TxnID>". Idempotency-keyed via `addEntityIdempotent`. Read-only gate composes (9001 from the InvoiceAdd half). Unknown sourceTxnId returns structured 500. Header-only sources allowed. 14 new tests in [tests/invoice-duplicate.test.ts](tests/invoice-duplicate.test.ts) cover happy path (single + multi-line, customer retarget by name and ListID, ClassRef/TermsRef carry, default Memo + Memo fallback, non-carry of TxnDate/DueDate/RefNumber, override-wins, header-only source, customer-balance composition with handleAdd), error paths (unknown TxnID 500, read-only 9001), and idempotency (same-key same-source replay + different-source 9002 conflict). Differs from `qb_estimate_convert_to_invoice` in carry policy (convert carries RefNumber from source; duplicate doesn't — different use case). Tool count 79 → 80. Build clean, 409/409 tests green (was 395 + 14 new).)_
- [ ] **58.** `qb_invoice_batch_create({ invoices: [...] })` and `qb_sales_receipt_batch_create({ receipts: [...] })` — atomic, all-or-nothing. Same envelope pattern as item 43 (batch JE). Replaces the Excel intermediate as a transport layer for high-volume entry.
- [ ] **59.** Attachments — attach a source PDF (vendor bill, deposit slip) to a transaction. QBXML: `AttachableAdd` against `AttachableRef`. For audit-trail use cases. Scope: tool that takes a `txnId` + local file path, uploads via the Attachable API, returns the attachment's `ListID`.

## Phase 13 — Data model gaps in returned objects

Beyond item 41 (line-level detail), these fields are currently missing or hard to reach.

- [ ] **60.** Customer email / phone in invoice list payload — currently to send a collection email you need a second `qb_customer_list` call per customer. Add `includeCustomerContact: boolean` arg, or join `Email`/`Phone` server-side from the customer cache when populated.
- [ ] **61.** Custom-field (DataExt) support across all entities. Most firms use them heavily (engagement type, tax year, partner assigned). Currently invisible. QBXML: `OwnerID` + `DataExtRet` round-trip. Add `includeCustomFields: boolean` and a separate `qb_custom_field_list` to discover what's defined per entity.
- [ ] **62.** Sub-customer / job hierarchy helpers. `CustomerRef.FullName` already encodes "Customer:Job" syntax but no traverse parent/child or filter-by-job helper exists. Add `qb_customer_jobs(parentListId)` and a `parentListID` / `jobOnly` filter on `qb_customer_list`.
- [ ] **63.** Memo / note full-text search across transactions — currently no way to search by memo content. QBXML doesn't support memo-substring filtering server-side, so wire as a server-side filter applied to `*Ret.Memo` after the QBXML response (with cost noted in the tool description: pulls all matching txns, then filters in-process).

## Phase 14 — Safety and DX

- [ ] **64.** Dry-run mode — `dryRun: true` on every `*_create` / `*_update` / `*_delete`. Returns "what would happen" with the validated payload (built QBXML envelope + simulation-store preview of resulting state) but does not commit. Today the only way to test a mutation is to do it.
- [ ] **65.** Better error surfaces. Today `statusCode -1` "QuickBooks found an error when parsing the provided XML text stream" is unactionable. Pass through the full QBXMLRP2 response, plus heuristic field hints — detect `"missing element: X"` / `"invalid argument: X"` / `"out of order: X"` patterns in the QB error message and surface the offending field name + the canonical schema-order for the parent element. Anchor on the `tests/builder-emit-order.test.ts` canonical sequences as the source of truth.
- [ ] **66.** `qb_audit_log({ txnId })` and `qb_audit_log({ dateRange })` — QB tracks who changed what; expose it. **Note:** QBXML SDK exposes audit-log read via `TxnReportQueryRq` (with audit-trail mode) only on Enterprise editions; Pro/Premier do not. Document the edition dependency; return a structured error on unsupported editions.
- [ ] **67.** `qb_session_status` — health check / probe. Verify SDK connection without performing a real query. Useful for orchestration retries. Returns `{ connected, mode, companyFile, qbxmlVersion, lastError, version }`.

## Phase 15 — Practice-specific wins (operator's firm + skills)

These directly upgrade the skills the operator has already built. Some are also product requirements that should land in [REQUIREMENTS.md](REQUIREMENTS.md) once scoped.

- [ ] **68.** `qb_trial_balance_export(asOfDate, basis)` — return TB in the exact column shape consumed by the operator's `trial-balance-workup` skill. Today the skill consumes a CSV exported from QB Desktop manually. Cutting that turns a manual hand-off into one tool call. Coordinate column names with the skill's parser before shipping.
- [ ] **69.** `qb_tax_line_mapping(accountListId?)` — return QB tax-line assignment (1120-S / 1065 / Sch C / Sch E / etc.) for any income/expense account. Bridge from books to tax software; currently rebuilt manually every workpaper. QBXML: `AccountQueryRq` already returns `TaxLineInfoRet`; just expose it.
- [ ] **70.** `qb_engagement_profitability(customerListId, dateRange)` — pulls revenue + time + reimbursable expenses for a job. Closer to ClosePilot's surface than the raw aging report. Composite that combines invoice/SR + time-tracking + expense-line queries.
- [ ] **71.** `qb_client_packet(customerListId, taxYear)` — bundles TB + GL + bank rec + fixed asset detail + payroll summary as one call. The workflow run 2,000 times per tax season. Composite that calls items 53 (GL), 68 (TB), 56 (bank rec discrepancy), payroll summary, fixed-asset list.

## Phase 16 — Architectural improvements

- [ ] **72.** Generic `qb_transaction_list({ types: [...], filters })` — replaces half the per-type list tools. "Show me everything for customer X in March" is currently 6 calls (invoice, SR, payment, CM, JE, statement charge); should be 1. Wire as a single `TransactionQueryRq` with the requested types under `IncludeAll`. Keep per-type tools as thin wrappers for compatibility.
- [ ] **73.** Streaming responses for large lists — a 2,000-customer dump should not require N pagination round trips when QB can produce one stream. MCP stdio supports response streaming via incremental notifications; investigate whether the MCP SDK's `server.tool` registration model supports it, or whether these tools need to drop below the helper.
- [ ] **74.** MCP-side caching of stable lookups (chart of accounts, customer list, item list, terms list, class list) with explicit invalidation. These barely change but currently cost a round trip every call. Add `useCache: boolean` (default `true`) and `qb_cache_invalidate({ entity })`. Cache lives in `QBSessionManager`, scoped to the current `companyFile`, cleared on `qb_company_open`.

## Phase 17 — Domain coverage gaps (entire areas missing today)

[CLAUDE.md](CLAUDE.md) lists `Deposit`, `Transfer`, `Check`, and `SalesOrder` as tracked transaction entities, but `src/tools/` has no banking.ts / sales-orders.ts / sales-tax.ts / time.ts / inventory.ts. These are entire workflow surfaces the MCP can't touch today. None block items 37-39 (P0 bugs) but each blocks a real practice workflow once core bugs are cleared.

- [ ] **75.** Banking primitives — `qb_deposit_create` / `qb_deposit_list` / `qb_deposit_update` / `qb_deposit_delete` (`DepositAddRq` / `DepositQueryRq` / `DepositModRq` / `TxnDelRq`); same CRUD for `Transfer` (`TransferInventoryAddRq` for inventory, plain `TransferAddRq` for cash) and `Check` (`CheckAddRq` covers vendor checks AND non-A/P bank disbursements). Without these, ad-hoc cash movement and bank deposits are invisible to the MCP — which means month-end bank rec (Phase 10 item 46) has no way to see deposits and transfers it's reconciling against.
- [ ] **76.** Sales orders — `qb_sales_order_create` / `_list` / `_update` / `_delete` / `_convert_to_invoice`. `SalesOrderAddRq` / `SalesOrderQueryRq` / `SalesOrderModRq`. **Distinct from estimates** (estimates are quotes; sales orders are committed but not yet invoiced — used for backorder / fulfillment workflows). Per [CLAUDE.md](CLAUDE.md) this is a tracked transaction entity but has no tool today.
- [ ] **77.** Sales tax workflows — completely absent. Add: `qb_sales_tax_liability_report` (monthly sales tax owed by agency); `qb_sales_tax_payment_create` (write the actual sales-tax check via `SalesTaxPaymentCheckAddRq`); `qb_sales_tax_code_list` / `qb_sales_tax_item_list` (discover existing codes and items so invoice/SR creation can reference them); `qb_sales_tax_agency_list`. For any client with taxable sales this is a monthly necessity.
- [ ] **78.** Time tracking — `qb_time_track_add` / `qb_time_track_list`. `TimeTrackingAddRq` / `TimeTrackingQueryRq`. Foundation for item 70 (`qb_engagement_profitability`) which can't compute hours-per-job without it. Also blocks any service-business billing-by-time workflow.
- [ ] **79.** Vehicle mileage — `qb_vehicle_mileage_add` / `_list` / `qb_vehicle_list`. `VehicleMileageAddRq` / `VehicleMileageQueryRq` / `VehicleQueryRq`. Tax-practice staple (Schedule C / Form 4562 mileage logs).
- [ ] **80.** Inventory adjustments — `qb_inventory_adjustment_add` / `_list`. `InventoryAdjustmentAddRq` / `InventoryAdjustmentQueryRq`. For shrinkage, count corrections, write-offs of damaged stock. Required for any client with inventory items (Phase 1 item 22 split the item store by type but never wired the adjustment surface).
- [ ] **81.** Statement charges — `qb_statement_charge_add` / `_list` / `_update` / `_delete`. `StatementChargeAddRq` / `StatementChargeQueryRq`. Mentioned in item 72 (generic transaction list) as one of the 6 transaction types but has no dedicated tool. Used by service businesses for time-and-materials billing without a formal invoice.

## Phase 18 — Engineering robustness

Operability gaps that bite when running unattended or diagnosing live failures.

- [ ] **82.** QB edition / version detection — `qb_host_query` exposes `HostQueryRq` results (`HostQueryRs.ProductName`, `MajorVersion`, `MinorVersion`, `Country`, `SupportedQBXMLVersionList`, `IsAutomaticLogin`, `QBFileMode`). Gates features by edition: item 55 (`qb_w2_summary` = Payroll subscription required), item 66 (`qb_audit_log` = Enterprise required). Surface edition in item 67 (`qb_session_status`) so agents can decide which tools are available without trial-and-error. Run on session connect; cache in `QBSessionManager`, cleared on `qb_company_open`.
- [ ] **83.** Persistent debug logging — `QB_DEBUG_QBXML=1` writes every QBXML request envelope + raw response to a rotating file (default `./logs/qbxml-YYYYMMDD.log`, configurable via `QB_DEBUG_LOG_PATH`). Needed to diagnose item 37 (P&L parse error) and every future schema-order-class bug — currently the only way to see the wire is to add `console.error` and rebuild. Redact any field flagged sensitive (vendor TIN, bank account number) before writing.
- [ ] **84.** Connection robustness — auto-reconnect on transient `QBXMLRP2` failures (status `0x80040408` "QBSession not open" surfaces when QB Desktop briefly stalls), exponential backoff (250ms → 500ms → 1s → fail) capped at 3 retries. Graceful `EndSession` + `CloseConnection` on `SIGTERM` / `SIGINT` so QB doesn't carry a stuck session into the next launch. Currently a Ctrl+C mid-call leaves QB Desktop with a half-open session that some operators have reported requires a QB restart to clear.
- [ ] **85.** Closing date / year-end lock — `qb_closing_date_set({ closingDate, password })` and `qb_closing_date_get`. `DataEventCallbackAddRq` / `PreferencesQueryRq` for read; the SDK exposure for *setting* the closing date is limited (varies by edition + version). Verify SDK surface before scoping; may need to fall back to documenting "set in QB Desktop" with a clear error in the tool. Audit-integrity critical for any practice that locks prior periods.
- [ ] **86.** MCP prompts (workflow bundles) — register pre-canned workflow guides via the MCP `prompts` API alongside `tools`. Examples: `monthly_close` (sequence of bank-rec → review uncleared → reconcile → run P&L / BS / cash flow → close period); `prep_1099` (list vendors over threshold → check W-9 status → generate 1099 forms → match to expense accounts); `engagement_intake` (create customer → set custom fields → create memorized invoice → schedule recurring billing). Helps agents navigate the right tool sequence without re-deriving the workflow each session, AND surfaces the operator's `credit-card-qb-batch` / `trial-balance-workup` / `cc-statement-validator` skill workflows directly in Claude Desktop.
